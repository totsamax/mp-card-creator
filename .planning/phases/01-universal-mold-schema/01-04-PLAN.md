---
phase: 01-universal-mold-schema
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - frontend/PipelineApp.jsx
autonomous: false
requirements: [INP-01, INP-02]
tags: [frontend, form, moldtype, formdata, mvp, ui]
user_setup: []

must_haves:
  truths:
    - "Форма опросника содержит select типа молда (Лицо/Руки/Обувь/Другое) — значение уходит в questionnaire.moldType"
    - "Форма позволяет выбрать ≥1 фото (File-объекты), которые отправляются в API"
    - "Отправка формы идёт как multipart/form-data (FormData) на POST /lines"
    - "Таблица размеров использует moldSize вместо faceSize"
    - "После успешного создания карточка возвращает article и добавляется в список"
  artifacts:
    - path: "frontend/PipelineApp.jsx"
      provides: "QuestionnaireForm с moldType select, photo file input (File-объекты), FormData submit; SIZE_DEFAULTS/SIZE_FIELDS на moldSize"
      contains: "moldType"
  key_links:
    - from: "frontend/PipelineApp.jsx QuestionnaireForm"
      to: "POST /lines (multipart)"
      via: "FormData с questionnaire(JSON) + photos(File[])"
      pattern: "FormData"
    - from: "QuestionnaireForm.buildQuestionnaire"
      to: "questionnaire.moldType"
      via: "form.moldType"
      pattern: "moldType"
---

<objective>
Завершить вертикальный слайс на UI: добавить в `QuestionnaireForm` (frontend/PipelineApp.jsx) select типа молда, file-input для фото (хранить File-объекты, не имена), переименовать `faceSize → moldSize` в `SIZE_DEFAULTS`/`SIZE_FIELDS`, и отправлять форму как `FormData` (multipart) на `POST /lines` — без ручного `Content-Type`, чтобы браузер выставил boundary. После этого пользователь может описать молд любого типа и приложить фото через форму end-to-end.

Purpose: INP-01 (загрузка фото), INP-02 (выбор типа). CONTEXT решение №5 (поля формы), RESEARCH (FormData без Content-Type, хранить File-объекты, переименовать ключ moldSize в форме иначе questionnaire не пройдёт обновлённую schema).
Output: рабочая форма создания карточки с типом и фото; ручная проверка в браузере (UI — checkpoint:human-verify).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-mold-schema/CONTEXT.md
@.planning/phases/01-universal-mold-schema/01-PATTERNS.md
@frontend/PipelineApp.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: moldType select + moldSize rename + photo File-input в QuestionnaireForm</name>
  <read_first>
    - frontend/PipelineApp.jsx (строки 594-751 — SIZE_DEFAULTS, SIZE_FIELDS, QuestionnaireForm; строки 11-18 apiFetch)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок PipelineApp.jsx, строки 396-513)
    - .planning/phases/01-universal-mold-schema/CONTEXT.md (решение №5 — поля формы)
  </read_first>
  <action>
    Per CONTEXT D-5 и PATTERNS. В `frontend/PipelineApp.jsx`:
    1. `SIZE_DEFAULTS` (строки 594-600): переименовать ключ `faceSize` → `moldSize` в каждой из 5 строк (значения оставить).
    2. `SIZE_FIELDS` (строки 602-608): первый элемент `{ key: 'faceSize', label: 'Личико, см' }` → `{ key: 'moldSize', label: 'Размер, см' }`.
    3. Начальный state формы (строки 611-616): добавить `moldType: 'face'` (дефолт, Pitfall 3 — без пустого значения). Заменить `const [files, setFiles] = useState([])` на `const [photoFiles, setPhotoFiles] = useState([])` — хранить File-объекты, не имена.
    4. Добавить select типа молда в JSX (рядом с полями color/theme, скопировать CSS-классы существующих input — `pp-label`, `pp-select` или `pp-input`): опции value face/hands/shoes/other, подписи Лицо/Руки/Обувь/Другое, `value={form.moldType}`, `onChange={(e)=>set('moldType', e.target.value)}`.
    5. Заменить блок «Рендеры молда» (строки 715-726) на блок «Фото молда»: `<input type="file" multiple accept="image/*" onChange={(e)=>setPhotoFiles(Array.from(e.target.files||[]))} />`; показывать счётчик `{photoFiles.length} файл(ов) выбрано`.
    6. В `buildQuestionnaire` (строки 625-639) добавить `moldType: form.moldType`. НЕ добавлять photos сюда — файлы пойдут отдельно в FormData (Task 2).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -Eqi "error|failed" && exit 1 || node -e "const s=require('fs').readFileSync('PipelineApp.jsx','utf8'); const ok=/moldType/.test(s) && /key: 'moldSize'/.test(s) && /setPhotoFiles/.test(s) && /accept=\"image\/\*\"/.test(s) && !/key: 'faceSize'/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `SIZE_DEFAULTS` и `SIZE_FIELDS` используют ключ `moldSize`, не `faceSize` (grep `key: 'faceSize'` == 0)
    - Начальный state содержит `moldType: 'face'`; state фото — `photoFiles` (File-объекты)
    - JSX содержит `<select>` с опциями face/hands/shoes/other и file input с `accept="image/*"` `multiple`
    - `buildQuestionnaire` возвращает объект с `moldType: form.moldType`
    - `npm run build` во frontend проходит без ошибок
  </acceptance_criteria>
  <done>Форма имеет select типа, file-input фото, таблица на moldSize; questionnaire несёт moldType</done>
</task>

<task type="auto">
  <name>Task 2: Отправка формы как FormData (multipart) на POST /lines</name>
  <read_first>
    - frontend/PipelineApp.jsx (строки 11-18 apiFetch; строки 842-866 handleFormSubmit; строка 746 onSubmit-кнопка; строка 957 рендер QuestionnaireForm)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (паттерн отправки FormData, строки 455-477)
    - .planning/phases/01-universal-mold-schema/01-RESEARCH.md (Code Examples — submitWithPhotos, Pitfall 5 CORS/Content-Type)
  </read_first>
  <action>
    Per RESEARCH (FormData без ручного Content-Type) и PATTERNS. В `frontend/PipelineApp.jsx`:
    1. Прокинуть выбранные File-объекты из формы в submit. Изменить кнопку `onSubmit(buildQuestionnaire())` (строка 746) так, чтобы передавались и questionnaire, и `photoFiles` (например `onSubmit(buildQuestionnaire(), photoFiles)`). Обновить сигнатуру `QuestionnaireForm({ onSubmit, loading })` — onSubmit принимает два аргумента.
    2. Добавить отдельную функцию отправки multipart (НЕ через apiFetch — apiFetch жёстко ставит `Content-Type: application/json`, что ломает boundary). Функция: `const fd = new FormData(); fd.append('questionnaire', JSON.stringify(questionnaire)); for (const file of photoFiles) fd.append('photos', file, file.name); const res = await fetch(\`${API_BASE}/lines\`, { method:'POST', body: fd });` БЕЗ headers. На `!res.ok` бросить ошибку с кодом статуса.
    3. Обновить `handleFormSubmit` (строки 842-866): принять второй аргумент `photoFiles`, вызвать новую multipart-функцию вместо `apiFetch('/lines', { method:'POST', body: JSON.stringify(questionnaire) })`. Сохранить существующее поведение: showToast, добавление линии в `setLines` (по `questionnaire.article` из ответа/формы), setActiveLineId, refreshManifest. На ошибку — showToast с текстом ошибки.
    4. Кнопка отправки должна быть disabled, если `photoFiles.length === 0` (INP-01 — фото обязательно, ≥1), в дополнение к существующим проверкам article/moldName.
  </action>
  <verify>
    <automated>cd frontend && grep -q "new FormData()" PipelineApp.jsx && grep -q "fd.append('photos'" PipelineApp.jsx && grep -q "fd.append('questionnaire'" PipelineApp.jsx && npm run build 2>&1 | grep -Eiq "error|failed" && exit 1 || echo build-ok</automated>
  </verify>
  <acceptance_criteria>
    - Существует FormData-отправка: `new FormData()`, `fd.append('questionnaire', JSON.stringify(...))`, `fd.append('photos', file, ...)` в цикле
    - Multipart-fetch вызывается БЕЗ ручного заголовка `Content-Type` (не через apiFetch)
    - `handleFormSubmit` больше не делает `apiFetch('/lines', { method:'POST', body: JSON.stringify(questionnaire) })`
    - Кнопка отправки disabled при `photoFiles.length === 0`
    - `npm run build` во frontend проходит без ошибок
  </acceptance_criteria>
  <done>Форма отправляет questionnaire+фото как multipart на POST /lines; JSON-отправка карточки удалена</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Браузерная проверка end-to-end создания карточки с типом и фото</name>
  <read_first>
    - .planning/phases/01-universal-mold-schema/01-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>
    Полный вертикальный слайс Phase 1: форма опросника с выбором типа молда и загрузкой фото → multipart POST /lines → схема/движок на moldType → сохранение фото и master-data в output/. Запускается локально через `npm run dev` (API :3001 + Vite :5173).
  </what-built>
  <how-to-verify>
    1. Запустить `npm run dev` (или `npm run api` + `cd frontend && npm run dev`). STORE_ADAPTER=local (или cloud-with-fallback — фолбэк на диск, ключей облака локально нет).
    2. Открыть http://localhost:5173, нажать «Новая карточка» / открыть форму опросника.
    3. Убедиться, что есть select «Тип молда» (Лицо/Руки/Обувь/Другое) и поле загрузки фото.
    4. Заполнить поля, выбрать тип `Руки`, приложить ≥1 изображение, заполнить таблицу размеров (колонка «Размер, см» — это moldSize), нажать «Сохранить и запустить пайплайн».
    5. Подтвердить: карточка появилась в списке без перезагрузки; в `output/{article}/photos/v1/` лежит загруженный файл; в `output/{article}/01-normalize/v1/master-data.json` titleFull НЕ содержит «личико» (тип hands).
  </how-to-verify>
  <resume-signal>Напишите "approved" если форма создаёт карточку с типом и фото корректно, либо опишите проблему</resume-signal>
  <verify>
    <human-check>Карточка с moldType=hands и фото создаётся через UI; master-data.json без «личико»; фото в output/</human-check>
  </verify>
  <acceptance_criteria>
    - В форме видны select типа молда и загрузка фото
    - Создание карточки с типом hands + фото проходит без ошибки в UI
    - Файл фото присутствует в output/{article}/photos/v1/
    - master-data.json для hands не содержит «личико»
    - Человек подтвердил ("approved")
  </acceptance_criteria>
  <done>Пользователь end-to-end описал молд типа hands и приложил фото через форму; карточка создана</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → POST /lines | пользовательский ввод и файлы формируются клиентом, валидируются на сервере (план 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | клиент может подделать FormData (тип/фото) | accept | server-side валидация в плане 03 (enum moldType через schema, mimeType image/*, sanitize filename) — клиентские проверки только UX; реальная защита на сервере |
| T-01-04-02 | Information Disclosure | ручной Content-Type ломает boundary → пустые поля | mitigate | FormData отправляется БЕЗ ручного Content-Type (браузер ставит boundary) — Task 2, Pitfall 5 |
</threat_model>

<verification>
- `npm run build` во frontend без ошибок
- grep: moldType/moldSize/FormData присутствуют; faceSize/JSON-POST карточки удалены
- Браузерная проверка (checkpoint): карточка hands+фото создаётся, master-data без «личико»
</verification>

<success_criteria>
- Форма даёт выбрать тип молда и приложить фото, отправляет multipart на POST /lines
- Таблица размеров на moldSize; questionnaire несёт moldType и проходит обновлённую schema
- End-to-end: пользователь создаёт карточку любого типа с фото через UI (vertical slice завершён)
</success_criteria>

<output>
Create `.planning/phases/01-universal-mold-schema/01-04-SUMMARY.md` when done
</output>
