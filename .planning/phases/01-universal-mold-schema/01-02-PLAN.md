---
phase: 01-universal-mold-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - input/questionnaire.schema.json
  - layers/shared/config/template.master.json
  - layers/shared/templateEngine.js
  - layers/shared/config/prompts.texts.json
autonomous: true
requirements: [INP-02, INP-03]
tags: [schema, template, moldtype, mvp]
user_setup: []

must_haves:
  truths:
    - "Опросник принимает moldType (face/hands/shoes/other) как обязательное поле"
    - "Опросник использует moldSize вместо faceSize в каждом размере"
    - "template.master.json вычисляет цену и тексты для любого moldType, тип — параметр, не отдельный шаблон"
    - "computeMasterData для moldType=hands не порождает текст со словом «личико»"
  artifacts:
    - path: "input/questionnaire.schema.json"
      provides: "схема с moldType (enum, required), photos (required, minItems 1), moldSize вместо faceSize"
      contains: "moldType"
    - path: "layers/shared/config/template.master.json"
      provides: "секция moldTypes (face/hands/shoes/other) + формулы на moldSize"
      contains: "moldTypes"
    - path: "layers/shared/templateEngine.js"
      provides: "чтение moldTypes[moldType] с fallback на static/textTemplates, moldSizeM вместо faceSizeM"
      exports: ["computeMasterData", "round", "evalExpr", "renderText"]
  key_links:
    - from: "layers/shared/templateEngine.js"
      to: "template.moldTypes[questionnaire.moldType]"
      via: "выбор шаблона titleFull/annotation/topic/purpose с fallback"
      pattern: "moldTypes"
    - from: "layers/shared/config/template.master.json"
      to: "computedFields.priceBase"
      via: "формула на moldSize/moldSizeM"
      pattern: "moldSize"
---

<objective>
Универсализировать схему данных: переименовать `faceSize → moldSize` и `renders → photos`, добавить обязательное поле `moldType`, добавить секцию `moldTypes` в `template.master.json` и научить `templateEngine.computeMasterData` брать `topic/purpose/titleFull/annotation` из `moldTypes[moldType]` с fallback на `static`/`textTemplates`. Этот слой делает тест A/B из плана 01 зелёным.

Purpose: D-решения CONTEXT №1 (faceSize→moldSize), №2 (moldType), №3 (renders→photos required), №4 (moldTypes секция + LLM-вариации). Это чистый backbone универсальности — без него тексты для рук/обуви получают шаблон «личико куклы» (нарушение INP-03 и будущего TXT-02).
Output: обновлённые schema + template + движок; unit-тесты templateEngine.test.js становятся GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-mold-schema/CONTEXT.md
@.planning/phases/01-universal-mold-schema/01-PATTERNS.md
@input/questionnaire.schema.json
@layers/shared/config/template.master.json
@layers/shared/templateEngine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить questionnaire.schema.json (moldType + photos + moldSize)</name>
  <read_first>
    - input/questionnaire.schema.json (полностью — текущая структура)
    - .planning/phases/01-universal-mold-schema/CONTEXT.md (решения №1, №2, №3)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок questionnaire.schema.json, строки 28-74)
  </read_first>
  <action>
    Per CONTEXT D-1/D-2/D-3. В `input/questionnaire.schema.json`:
    1. В `sizes.items.required` (строка 46) заменить `"faceSize"` → `"moldSize"`.
    2. В `sizes.items.properties` переименовать ключ `faceSize` → `moldSize`, заменить `description` на `"Характерный размер молда, см"`.
    3. Добавить в корневой `properties` (после `color`) поле `moldType` типа string с `enum: ["face","hands","shoes","other"]` и описанием `"Тип молда — определяет шаблонные тексты и топик"`.
    4. Добавить `"moldType"` в корневой массив `required` (строка 6).
    5. Заменить блок `renders` (строки 81-90) на `photos`: type array, `minItems: 1`, `maxItems: 10`, items string с `minLength: 1`, description `"URL фотографий молда в Object Storage (≥1, разные ракурсы)"`. Удалить `renders` полностью (schema имеет `additionalProperties: false` — лишнее поле сломает валидацию).
    6. Добавить `"photos"` в корневой `required`. Оставить `"artifacts"` в required как есть (CONTEXT open question №3 — минимизируем изменения, форма уже шлёт artifacts).
  </action>
  <verify>
    <automated>node -e "const s=require('./input/questionnaire.schema.json'); const ok = s.properties.moldType && s.properties.photos && s.properties.photos.minItems===1 && s.required.includes('moldType') && s.required.includes('photos') && !s.properties.renders && s.properties.sizes.items.properties.moldSize && !s.properties.sizes.items.properties.faceSize && s.properties.sizes.items.required.includes('moldSize'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `properties.moldType.enum` равен `["face","hands","shoes","other"]`; `moldType` в корневом `required`
    - `properties.photos.minItems === 1`, `properties.photos.maxItems === 10`; `photos` в корневом `required`
    - `properties.renders` отсутствует
    - `sizes.items.properties.moldSize` существует, `faceSize` отсутствует; `sizes.items.required` содержит `moldSize`, не `faceSize`
    - JSON валиден (`require` не падает)
  </acceptance_criteria>
  <done>Схема принимает moldType и photos как обязательные, использует moldSize</done>
</task>

<task type="auto">
  <name>Task 2: Добавить секцию moldTypes и переименовать формулы в template.master.json</name>
  <read_first>
    - layers/shared/config/template.master.json (полностью)
    - .planning/phases/01-universal-mold-schema/CONTEXT.md (решение №4 — полная структура moldTypes)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок template.master.json, строки 78-124)
  </read_first>
  <action>
    Per CONTEXT D-4. В `layers/shared/config/template.master.json`:
    1. В `computedFields`: `priceBase` заменить `faceSize / faceSizeM` → `moldSize / moldSizeM` (формула `round(priceBaseM * (moldSize / moldSizeM), 10)`); `toyFrom` `faceSize * 4` → `moldSize * 4`; `toyTo` `faceSize * 8` → `moldSize * 8`.
    2. В `textTemplates`: `titleShort` `{{faceSize}}` → `{{moldSize}}`. `titleFull` и `annotation` заменить на нейтральные placeholder-ы без слова «личико» (конкретные тексты переезжают в moldTypes) — например titleFull `"Молд силиконовый «{{moldName}}», {{moldLength}}x{{moldWidth}}x{{moldHeight}} см, {{brand}}"`, annotation `"Авторский силиконовый молд для ватных игрушек и рукоделия."` Это fallback-значения.
    3. Добавить новую секцию верхнего уровня `moldTypes` с ключами `face`, `hands`, `shoes`, `other`. Каждый ключ — объект с полями `topic`, `purpose`, `titleFull`, `annotation`, взятыми ДОСЛОВНО из CONTEXT.md решение №4 (строки 84-109). titleFull-шаблоны используют токены `{{moldName}}`, `{{moldLength}}`, `{{moldWidth}}`, `{{moldHeight}}`, `{{brand}}`.
    4. `static.topic` и `static.purpose` оставить без изменений (дефолт для fallback).
  </action>
  <verify>
    <automated>node -e "const t=require('./layers/shared/config/template.master.json'); const ok = t.moldTypes && ['face','hands','shoes','other'].every(k=>t.moldTypes[k]&&t.moldTypes[k].titleFull&&t.moldTypes[k].topic) && /moldSize/.test(t.computedFields.priceBase) && !/faceSize/.test(JSON.stringify(t.computedFields)) && !/личико/.test(t.textTemplates.titleFull); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `moldTypes.face`, `moldTypes.hands`, `moldTypes.shoes`, `moldTypes.other` существуют, каждый имеет `topic`, `purpose`, `titleFull`, `annotation`
    - `computedFields.priceBase` содержит `moldSize` и `moldSizeM`, не содержит `faceSize`
    - `computedFields.toyFrom`/`toyTo` используют `moldSize`
    - `textTemplates.titleFull` не содержит подстроку «личико»
    - `moldTypes.face.titleFull` содержит «личик» (face-специфичный текст сохранён в типе)
  </acceptance_criteria>
  <done>Секция moldTypes покрывает все 4 типа; формулы на moldSize; fallback-шаблоны нейтральны</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Научить templateEngine читать moldTypes с fallback (GREEN для тестов A/B)</name>
  <read_first>
    - layers/shared/templateEngine.js (полностью — функция computeMasterData, строки 49-104)
    - layers/shared/config/prompts.texts.json (строка с {{faceSize}} — переименовать для синхронизации мастер-данных)
    - test/templateEngine.test.js (созданный в плане 01 — целевые ассерты A/B)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок templateEngine.js, строки 128-208)
  </read_first>
  <behavior>
    - Тест A (из плана 01): computeMasterData с moldType='hands' и sizes[].moldSize → priceBase для M > 0, titleFull без «личико» → должен стать GREEN.
    - Тест B (из плана 01): moldType отсутствует/неизвестен → topic берётся из static.topic, исключения нет → GREEN.
  </behavior>
  <action>
    Per CONTEXT D-1/D-4 и PATTERNS Pattern 3. В `layers/shared/templateEngine.js`:
    1. Строка 58: `const faceSizeM = baseRow.faceSize` → `const moldSizeM = baseRow.moldSize`.
    2. Строка 73 (ctx): ключ `faceSizeM` → `moldSizeM`.
    3. В `computeMasterData` после деструктуризации добавить выбор typeCfg: `const typeCfg = (template.moldTypes && questionnaire.moldType) ? template.moldTypes[questionnaire.moldType] : null;` и из него `titleFullTmpl = typeCfg?.titleFull ?? template.textTemplates.titleFull`, `annotationTmpl = typeCfg?.annotation ?? template.textTemplates.annotation`, `topic = typeCfg?.topic ?? template.static.topic`, `purpose = typeCfg?.purpose ?? template.static.purpose`.
    4. В цикле рендера текстов (строки 82-85) для поля `titleFull` использовать `titleFullTmpl`, для `annotation` — `annotationTmpl`, для остальных — оригинальный tmpl.
    5. В возвращаемую запись (строки 87-103) добавить `topic` и `purpose` из вычисленных выше (перекрывая значения из ...staticFields для согласованности с выбранным типом).
    6. Деструктурировать `moldType` из questionnaire рядом с другими скалярами (для прокидывания в выходную запись, опционально, но moldType должен быть доступен).
    7. Дополнительно (синхронизация, A3): в `layers/shared/config/prompts.texts.json` заменить `{{faceSize}}` → `{{moldSize}}`, чтобы промпт совпадал с новыми мастер-данными (Phase 2 это использует, но рассинхрон вреден сразу).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "fail 0" || node --test test/templateEngine.test.js 2>&1 | grep -Eq "pass [1-9]|ok [0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `templateEngine.js` не содержит идентификатор `faceSize` (grep `grep -v '^[[:space:]]*//' layers/shared/templateEngine.js | grep -c faceSize` == 0)
    - `computeMasterData` для moldType='hands' возвращает запись, где `titleFull` не содержит «личико»
    - `computeMasterData` без moldType не выбрасывает исключение и даёт `topic === template.static.topic`
    - `node --test test/templateEngine.test.js` — тесты A и B проходят (pass)
    - `prompts.texts.json` не содержит `{{faceSize}}`
  </acceptance_criteria>
  <done>Движок параметризован по moldType с fallback; unit-тесты GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| questionnaire (внешний ввод) → templateEngine | moldType из формы попадает в выбор шаблона |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `moldType` вне enum | mitigate | schema enum ограничивает значения; templateEngine fallback на static при неизвестном типе (не падает, не утекает) — Task 1, Task 3 |
| T-01-02-02 | Information Disclosure | `evalExpr` (new Function) над computedFields | accept | формулы — данные конфига template.master.json (доверенный), не пользовательский ввод; ctx содержит только числовые/строковые поля опросника. Существующий паттерн, риск не вводится этой фазой |
</threat_model>

<verification>
- `node -e` проверки schema и template проходят
- `node --test test/templateEngine.test.js` — тесты A/B GREEN
- grep: ни faceSize в templateEngine.js, ни «личико» в textTemplates.titleFull
</verification>

<success_criteria>
- moldType — обязательное enum-поле; photos — обязательное; moldSize заменил faceSize везде в data-слое
- moldTypes покрывает 4 типа; движок выбирает шаблон по типу с fallback
- Юнит-тесты templateEngine из плана 01 зелёные
</success_criteria>

<output>
Create `.planning/phases/01-universal-mold-schema/01-02-SUMMARY.md` when done
</output>
