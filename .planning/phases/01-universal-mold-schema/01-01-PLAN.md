---
phase: 01-universal-mold-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/templateEngine.test.js
  - test/create-line.smoke.test.js
  - test/fixtures/test-mold.png
autonomous: false
requirements: [INP-01, INP-02, INP-03]
tags: [multipart, schema, busboy, mvp, walking-skeleton]
user_setup: []

must_haves:
  truths:
    - "Проект имеет рабочий тест-раннер (node:test) — npm test запускается без exit 1"
    - "Существует падающий end-to-end тест happy-path: POST /lines с moldType+photos создаёт карточку"
    - "busboy установлен и подтверждён как легитимный пакет перед npm install"
  artifacts:
    - path: "test/templateEngine.test.js"
      provides: "unit-тест moldType-параметризации и moldSize-rename (изначально RED)"
      min_lines: 20
    - path: "test/create-line.smoke.test.js"
      provides: "integration smoke-тест multipart POST /lines (изначально RED)"
      min_lines: 25
    - path: "test/fixtures/test-mold.png"
      provides: "фикстура-изображение для multipart-загрузки"
    - path: "package.json"
      provides: "test-скрипт node:test + зависимость busboy"
      contains: "busboy"
  key_links:
    - from: "package.json"
      to: "node --test"
      via: "scripts.test"
      pattern: "node --test"
---

<objective>
Поставить фундамент валидации Phase 1: установить busboy (с проверкой легитимности), добавить тест-раннер `node:test` (без новых зависимостей сверх busboy) и написать ДВА падающих теста, описывающих целевое поведение всей фазы — параметризацию `moldType` в `templateEngine` и end-to-end создание карточки через multipart `POST /lines` с фото.

Purpose: Phase 1 — Walking Skeleton первой планируемой фазы. Vertical-slice MVP-правило требует, чтобы первый не-тестовый артефакт давал рабочий end-to-end путь, а первая задача — падающий happy-path тест. Эти тесты — контракт, который реализуют планы 02/03/04: при их прохождении INP-01/02/03 доказаны автоматически.
Output: рабочий `npm test`, два RED-теста, фикстура-изображение, установленный busboy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-mold-schema/CONTEXT.md
@.planning/phases/01-universal-mold-schema/01-VALIDATION.md
@layers/shared/templateEngine.js
@layers/shared/config/template.master.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Проверить легитимность busboy и установить (T-01-SC)</name>
  <read_first>
    - .planning/phases/01-universal-mold-schema/01-RESEARCH.md (раздел Package Legitimacy Audit, строки 57-66)
    - package.json (текущие dependencies)
  </read_first>
  <what-built>
    Перед установкой: автоматически выполнить `npm view busboy version` и `npm view busboy scripts.postinstall` — зафиксировать, что версия `1.6.0` (или новее в 1.x), а postinstall-скрипт пуст. RESEARCH.md помечает busboy как `[ASSUMED]` (имя из training data) → требуется блокирующий human-verify перед install (T-01-SC threat).
  </what-built>
  <how-to-verify>
    1. Открыть https://www.npmjs.com/package/busboy — подтвердить: автор `mscdex`, репозиторий github.com/mscdex/busboy, последняя версия в линейке 1.x, высокие загрузки (это транзитивная зависимость multer/express-экосистемы).
    2. Убедиться, что у пакета НЕТ подозрительного postinstall-скрипта.
    3. После одобрения выполнить `npm install busboy@^1.6.0` в корне проекта.
  </how-to-verify>
  <resume-signal>Напишите "approved" если busboy подтверждён как легитимный, либо опишите проблему</resume-signal>
  <verify>
    <automated>node -e "require('busboy'); console.log('busboy ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('busboy')"` выполняется без ошибки модуля
    - `package.json` содержит `busboy` в `dependencies` с версией `^1.6.0`
    - Чекпойнт одобрен человеком (legitimacy gate не авто-проходимый)
  </acceptance_criteria>
  <done>busboy установлен, импортируется, подтверждён человеком как легитимный пакет</done>
</task>

<task type="auto">
  <name>Task 2: Добавить тест-раннер node:test и фикстуру-изображение</name>
  <read_first>
    - package.json (текущий блок scripts — `"test": "echo ... && exit 1"`)
    - layers/shared/templateEngine.js (экспорт computeMasterData для unit-теста)
  </read_first>
  <action>
    Заменить в `package.json` скрипт `test` со значения `echo "Error: no test specified" && exit 1` на `node --test test/`. Node 22 (подтверждено RESEARCH.md: v22.22.1) поддерживает встроенный `node:test` без новых зависимостей. Создать директорию `test/fixtures/` и положить туда минимальный валидный PNG `test/fixtures/test-mold.png` (сгенерировать через node: записать буфер минимального 1x1 PNG, либо использовать `Buffer.from` с известной PNG-сигнатурой — файл должен быть распознаваемым как image/png, размер не важен). Это фикстура для multipart-загрузки в smoke-тесте.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -vq "no test specified" && node -e "const fs=require('fs'); const b=fs.readFileSync('test/fixtures/test-mold.png'); process.exit(b.length>=8 && b[0]===0x89 && b[1]===0x50 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.test` равен строке, содержащей `node --test`
    - `npm test` НЕ выводит `no test specified` и не падает с `exit 1` из-за отсутствия раннера (может падать на RED-тестах из Task 3 — это ожидаемо)
    - Файл `test/fixtures/test-mold.png` существует, начинается с PNG-сигнатуры (байты `0x89 0x50 0x4E 0x47`)
  </acceptance_criteria>
  <done>node:test подключён как раннер, фикстура-PNG создана</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Написать два падающих теста (RED) — контракт фазы</name>
  <read_first>
    - layers/shared/templateEngine.js (сигнатура computeMasterData(questionnaire, template))
    - layers/shared/config/template.master.json (текущая структура — НЕТ секции moldTypes)
    - input/questionnaire.schema.json (текущая структура — поле faceSize, нет moldType/photos)
    - functions/api/index.js (handleCreateLine, строки 181-231 — текущий JSON-only путь)
    - .planning/phases/01-universal-mold-schema/01-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <behavior>
    - test/templateEngine.test.js:
      - Тест A: computeMasterData с questionnaire где `sizes[].moldSize` (НЕ faceSize) и `moldType:'hands'` возвращает 5 записей; `priceBase` для M > 0; `titleFull` НЕ содержит подстроку «личико». (RED сейчас: template.master.json не имеет moldTypes, движок читает baseRow.faceSize → undefined.)
      - Тест B: при `moldType` отсутствующем/неизвестном computeMasterData делает fallback на static-значения (topic = static.topic) без выброса исключения.
    - test/create-line.smoke.test.js:
      - Тест C: импортирует `handler` из functions/api, формирует event с `files:[{filename,mimeType,buffer}]` и `formFields.questionnaire` (JSON с moldType+sizes[].moldSize), вызывает handler — ожидает statusCode 200 и тело с `stepId:'01-normalize'`. (RED сейчас: handleCreateLine не читает event.files/event.formFields.)
      - Тест проверяет, что после вызова в `questionnaire.photos[]` (через ответ/манифест) непустой массив ссылок.
  </behavior>
  <action>
    Создать `test/templateEngine.test.js` через `node:test` (`const { test } = require('node:test'); const assert = require('node:assert')`). Импортировать `computeMasterData` из `../layers/shared/templateEngine` и `template` из `../layers/shared/config/template.master.json`. Написать тесты A и B по behavior выше. Использовать STORE_ADAPTER=local через `process.env.STORE_ADAPTER='local'` и временный OUTPUT_DIR в smoke-тесте, чтобы не трогать облако. В `test/create-line.smoke.test.js` собрать event вручную (читать фикстуру `test/fixtures/test-mold.png` как buffer), вызвать `handler(event)`, распарсить `result.body`. Тесты ДОЛЖНЫ падать сейчас (RED) — это фиксирует контракт для планов 02/03/04. НЕ менять production-код в этом плане.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -Eq "fail|not ok|AssertionError" && echo "RED confirmed (expected)" || (echo "tests unexpectedly pass — contract not RED" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `test/templateEngine.test.js` содержит ≥2 теста (A: moldType=hands без «личико»; B: fallback на static)
    - `test/create-line.smoke.test.js` содержит тест C, импортирующий `handler` из `../functions/api` и подающий event с `files` и `formFields.questionnaire`
    - `npm test` сейчас завершается с провалом (RED) — вывод содержит `fail`/`not ok`/`AssertionError`
    - В тестах НЕ изменён ни один файл из production-кода (templateEngine.js, api/index.js, schema, template.master.json остаются как были)
  </acceptance_criteria>
  <done>Два RED-теста описывают целевое поведение фазы; npm test падает строго на этих тестах</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Установка стороннего пакета busboy выполняет код при install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install busboy` | mitigate | blocking-human checkpoint перед install; verify через npmjs.com/package/busboy (автор mscdex, postinstall пуст) — Task 1 |
</threat_model>

<verification>
- `npm test` запускается через node:test (нет `no test specified`)
- Два теста существуют и падают (RED) — контракт фазы зафиксирован
- busboy импортируется
</verification>

<success_criteria>
- Тест-раннер работает, busboy установлен и проверен
- Падающие тесты A/B/C точно описывают INP-01/02/03 в исполняемой форме
- Никакой production-код не тронут (этот план — только леса валидации)
</success_criteria>

<output>
Create `.planning/phases/01-universal-mold-schema/01-01-SUMMARY.md` when done
</output>
