---
phase: 01-universal-mold-schema
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - infra/local-server.js
  - functions/api/index.js
autonomous: true
requirements: [INP-01, INP-02]
tags: [multipart, busboy, upload, api, mvp]
user_setup: []

must_haves:
  truths:
    - "POST /lines с multipart/form-data (поля + файлы) создаёт карточку без ошибки"
    - "Каждое загруженное фото сохраняется через versionStore в {article}/photos/v1/{filename}"
    - "Имена файлов санитизируются (path.basename + whitelist) перед сохранением — нет path traversal"
    - "questionnaire.photos[] заполняется ссылками ДО вычисления inputHash"
    - "JSON-путь POST /lines (без файлов) продолжает работать как раньше"
  artifacts:
    - path: "infra/local-server.js"
      provides: "ветка multipart-парсинга через busboy, прокидывает event.files + event.formFields"
      contains: "multipart/form-data"
    - path: "functions/api/index.js"
      provides: "handleCreateLine принимает event.files, сохраняет фото через putArtifact, подставляет photos[]"
      contains: "event.files"
  key_links:
    - from: "infra/local-server.js"
      to: "functions/api handleCreateLine"
      via: "event.files / event.formFields"
      pattern: "formFields"
    - from: "functions/api/index.js"
      to: "store.putArtifact(article, 'photos', 1, ...)"
      via: "загрузка фото как артефакта"
      pattern: "putArtifact\\([^,]+,\\s*'photos'"
---

<objective>
Принять `multipart/form-data` в `POST /lines`: распарсить поток через busboy в `infra/local-server.js` (HTTP-адаптер владеет `req`), прокинуть `event.files` + `event.formFields` в handler, а в `handleCreateLine` — сохранить каждое фото через `store.putArtifact(article,'photos',1,...)`, санитизировать имена файлов и подставить ссылки в `questionnaire.photos[]` ДО `inputHash`. Это wiring, который делает smoke-тест C из плана 01 зелёным.

Purpose: INP-01 (фото — обязательная часть опросника). CONTEXT решение №3 определяет multipart как основной путь. RESEARCH: парсить на уровне адаптера (serverless-совместимость), фото грузить через versionStore (фолбэк бесплатно), не писать прямой S3-код.
Output: рабочий multipart POST /lines; smoke-тест create-line.smoke.test.js GREEN; JSON-путь не сломан.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-universal-mold-schema/CONTEXT.md
@.planning/phases/01-universal-mold-schema/01-RESEARCH.md
@.planning/phases/01-universal-mold-schema/01-PATTERNS.md
@infra/local-server.js
@functions/api/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Парсить multipart в local-server.js через busboy</name>
  <read_first>
    - infra/local-server.js (полностью — блок "Collect request body" строки 56-70 и event-сборка)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок infra/local-server.js, строки 295-392)
    - .planning/phases/01-universal-mold-schema/01-RESEARCH.md (Pattern 1, строки 115-147; Pitfall 5 CORS, строки 248-252)
  </read_first>
  <action>
    Per RESEARCH Pattern 1. В `infra/local-server.js` заменить безусловный блок чтения тела (строки 56-70 — `for await (const chunk of req)` + `.toString()`) на ветвление по заголовку `Content-Type`:
    1. Прочитать `const ct = req.headers['content-type'] || ''`.
    2. Если `ct.startsWith('multipart/form-data')`: подключить `const Busboy = require('busboy')` (lazy require внутри ветки), создать `Busboy({ headers: req.headers, limits: { fileSize: 15*1024*1024, files: 10 } })`. Собрать в Promise: на событие `field` копить `fields[name]=val`; на событие `file` собирать чанки стрима в `Buffer.concat(chunks)` и пушить `{ field, filename: info.filename, mimeType: info.mimeType, buffer }`; на `close` — resolve `{fields,files}`; на `error` — reject. `req.pipe(bb)`. Собрать `event` с новыми полями `formFields: fields`, `files`, `body: null`, плюс существующие `httpMethod/url/path/queryStringParameters/headers/isBase64Encoded:false`.
    3. Иначе (JSON-путь) — оставить существующую логику чтения чанков и `.toString()` без изменений, собрать тот же `event` что и раньше (с `body: rawBody`).
    4. НЕ трогать busboy-ошибки молча — reject пробрасывается в существующий `.catch` обработчика (строки 74-77) → 500.
    5. CORS: НЕ менять `corsHeaders` — `Access-Control-Allow-Headers: Content-Type` достаточно для FormData (Pitfall 5).
  </action>
  <verify>
    <automated>node -e "require('./infra/local-server.js')" & SVRPID=$!; sleep 1; printf 'a' > /tmp/mc-mold.png; RESP=$(curl -s -o /dev/null -w '%{http_code}' -F 'questionnaire={\"article\":\"mc-smoke\",\"moldName\":\"T\",\"brand\":\"B\",\"theme\":\"t\",\"color\":\"c\",\"moldType\":\"hands\",\"priceBaseM\":1000,\"artifacts\":[\"images\"],\"sizes\":[{\"size\":\"XS\",\"moldSize\":2,\"moldLength\":3,\"moldWidth\":1.25,\"moldHeight\":2.5,\"moldWeight\":7},{\"size\":\"S\",\"moldSize\":3,\"moldLength\":4.5,\"moldWidth\":1.9,\"moldHeight\":3.75,\"moldWeight\":25},{\"size\":\"M\",\"moldSize\":4,\"moldLength\":6,\"moldWidth\":2.5,\"moldHeight\":5,\"moldWeight\":60},{\"size\":\"L\",\"moldSize\":5,\"moldLength\":7.5,\"moldWidth\":3.1,\"moldHeight\":6.25,\"moldWeight\":117},{\"size\":\"XL\",\"moldSize\":6,\"moldLength\":9,\"moldWidth\":3.75,\"moldHeight\":7.5,\"moldWeight\":202}]}' -F 'photos=@/tmp/mc-mold.png' http://localhost:3001/lines); kill $SVRPID; test "$RESP" = "200"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/local-server.js` содержит ветку `ct.startsWith('multipart/form-data')`
    - В multipart-ветке event получает поля `formFields` и `files` (массив `{filename,mimeType,buffer}`)
    - JSON-ветка по-прежнему собирает `event.body = rawBody` через `.toString()`
    - `curl -F` multipart POST /lines с валидным questionnaire (STORE_ADAPTER=local) возвращает 200
  </acceptance_criteria>
  <done>Сервер парсит multipart через busboy и прокидывает files/formFields в handler; JSON-путь цел</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handleCreateLine принимает event.files, сохраняет фото, подставляет photos[] (GREEN для теста C)</name>
  <read_first>
    - functions/api/index.js (handleCreateLine строки 181-231 + require path/store строки 4-7)
    - .planning/phases/01-universal-mold-schema/01-PATTERNS.md (блок handleCreateLine, строки 212-291; Pattern 2 фото-как-артефакт)
    - .planning/phases/01-universal-mold-schema/01-RESEARCH.md (Pitfall 1 toString, Pitfall 4 inputHash, Security path traversal строки 400-409)
    - test/create-line.smoke.test.js (целевой ассерт C из плана 01)
  </read_first>
  <behavior>
    - Тест C (из плана 01): event с `files:[{filename,mimeType,buffer}]` + `formFields.questionnaire` (JSON с moldType+moldSize) → handler возвращает statusCode 200, тело с `stepId:'01-normalize'`; `questionnaire.photos[]` непустой. → GREEN.
  </behavior>
  <action>
    Per CONTEXT D-3, RESEARCH Pattern 2 и Security. В `functions/api/index.js` → `handleCreateLine`:
    1. В начало функции (до текущего `let body`) добавить ветку: если `event.files && event.files.length > 0` — распарсить `questionnaire = JSON.parse(event.formFields.questionnaire || '{}')` (на ошибку парсинга вернуть `respond(400,{error:'Invalid questionnaire JSON in form field'})`); прочитать `force = event.formFields.force === 'true'`.
    2. Для каждого `f` в `event.files`: санитизировать имя `const safeName = path.basename(f.filename).replace(/[^a-zA-Z0-9._-]/g, '_')` (защита от path traversal — `versionStore.artifactPath` строит путь из имени). Если `safeName` пустой после санитизации — вернуть 400. Дополнительно отклонять не-изображения: если `f.mimeType` не начинается с `image/` — `respond(400,{error:'Only image uploads allowed'})`. Вызвать `await store.putArtifact(questionnaire.article, 'photos', 1, safeName, f.buffer)`; накопить `photoRefs.push('/lines/'+questionnaire.article+'/steps/photos/artifacts/'+safeName)`.
    3. После цикла: `questionnaire.photos = photoRefs` — ДО вычисления inputHash (Pitfall 4: фото должны войти в хэш).
    4. Иначе (нет files) — выполнить существующий JSON-путь без изменений: распарсить body, `({ force=false, ...questionnaire } = body)`.
    5. Унифицировать ниже по коду: дальше код работает с переменными `questionnaire` и `force` (которые теперь заданы в обеих ветках). Убедиться, что существующая проверка `if (!article)` (строка 194) и весь inputHash/cache/putArtifact-flow ниже использует общую `questionnaire`. `path` уже импортирован (строка 4) — переиспользовать, не дублировать require.
    6. Ошибки busboy/загрузки не глотать: проброс через top-level try/catch handler → 500.
  </action>
  <verify>
    <automated>STORE_ADAPTER=local OUTPUT_DIR=/tmp/mc-out node --test test/create-line.smoke.test.js 2>&1 | grep -Eq "pass [1-9]|# pass [1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `handleCreateLine` содержит ветку `if (event.files && event.files.length > 0)`
    - Имя файла санитизируется через `path.basename(...).replace(/[^a-zA-Z0-9._-]/g, '_')` перед putArtifact
    - Фото сохраняются через `store.putArtifact(..., 'photos', 1, safeName, f.buffer)` — НЕ прямой S3-вызов (grep `new S3Client` в handleCreateLine == 0)
    - `questionnaire.photos` присваивается до `crypto.createHash` inputHash
    - Не-image mimeType → 400; пустое имя после санитизации → 400
    - `node --test test/create-line.smoke.test.js` (STORE_ADAPTER=local) — тест C проходит (pass)
    - JSON-путь без файлов: `({ force=false, ...questionnaire } = body)` сохранён
  </acceptance_criteria>
  <done>handleCreateLine обрабатывает multipart, безопасно сохраняет фото, заполняет photos[]; smoke-тест GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /lines (multipart) | непроверенный ввод: имена файлов, mimeType, содержимое, поля формы пересекают границу |
| handleCreateLine → versionStore.artifactPath | filename используется для построения пути на диске/в Object Storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | path traversal в `filename` (`../../etc`) | mitigate | `path.basename(filename).replace(/[^a-zA-Z0-9._-]/g,'_')` перед putArtifact; пустое имя → 400 — Task 2 |
| T-01-03-02 | Tampering | инъекция полей формы (только known fields из formFields) | mitigate | handleCreateLine читает только `formFields.questionnaire` и `formFields.force`; прочие поля игнорируются; questionnaire парсится как JSON и проходит computeMasterData — Task 2 |
| T-01-03-03 | Tampering | загрузка не-изображения под видом фото | mitigate | проверка `mimeType` начинается с `image/` на сервере (дублирует frontend accept) — Task 2 |
| T-01-03-04 | Denial of Service | гигантские файлы / много файлов | mitigate | busboy `limits: { fileSize: 15MB, files: 10 }` — Task 1 |
</threat_model>

<verification>
- `curl -F` multipart POST /lines → 200 (STORE_ADAPTER=local)
- `node --test test/create-line.smoke.test.js` — тест C GREEN
- Фото появляется в `output/{article}/photos/v1/{safeName}` (local mode)
- JSON POST /lines по-прежнему работает (регрессия не введена)
</verification>

<success_criteria>
- multipart POST /lines создаёт карточку, сохраняет фото безопасно, заполняет photos[]
- inputHash включает photos (смена фото инвалидирует кэш)
- smoke-тест из плана 01 зелёный; JSON-путь не сломан
</success_criteria>

<output>
Create `.planning/phases/01-universal-mold-schema/01-03-SUMMARY.md` when done
</output>
