---
phase: 05-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e.test.js
  - test/fixtures/e2e-face-questionnaire.json
  - package.json
autonomous: true
requirements: [REL-02]
user_setup:
  - service: openai
    why: "E2E делает реальные вызовы OpenAI для генерации текстов и слайдов (D-01)"
    env_vars:
      - name: OPENAI_API_KEY
        source: ".env.local в корне проекта (наследуется spawned-сервером) — НЕ хардкодить в тесте"
    dashboard_config:
      - task: "Положить реальное фото лицевого молда в test/fixtures/e2e-face-mold.png (PNG, > 1000 байт)"
        location: "Локальная файловая система — D-04, фото предоставляет пользователь перед прогоном"

must_haves:
  truths:
    - "RUN_E2E=1 npm run test:e2e прогоняет весь pipeline 01→06 через живой HTTP-сервер и завершается exit 0 (REL-02)"
    - "Без RUN_E2E обычный npm test НЕ запускает платный E2E (skip-guard) — реальные OpenAI-вызовы не идут на каждом коммите"
    - "E2E поднимает infra/local-server.js дочерним процессом на порту 3101 со STORE_ADAPTER=local и изолированным OUTPUT_DIR=test/e2e-output, гарантированно убивает его в after()"
    - "POST /lines с multipart (фото + questionnaire moldType:face) возвращает 200 и stepId 01-normalize (D-03)"
    - "Async-шаги (02-texts, 03-images) поллятся по artifact-count==5 и steps[stepId].error, НЕ по несуществующему status-полю"
    - "После 06-assemble на диске существуют: master-data.json, 5×{size}_texts.json без нераскрытых {{}}, 5×{size}_infographic.png размером > 1000 байт, {article}_ozon.xlsx и {article}_wb.xlsx открываются через ExcelJS без ошибок, assemble-report.json"
    - "Preflight-чек падает с понятным сообщением, если OPENAI_API_KEY не задан или фото-фикстура отсутствует/слишком мала"
  artifacts:
    - path: "test/e2e.test.js"
      provides: "E2E-оркестратор: spawn server, wait-for-port, POST /lines, последовательный запуск шагов, pollStep, валидация артефактов"
      min_lines: 150
      contains: "pollStep"
    - path: "test/fixtures/e2e-face-questionnaire.json"
      provides: "Шаблон опросника лицевого молда (moldType:face, sizes[5]) — article подставляется в рантайме"
      contains: "\"moldType\": \"face\""
    - path: "package.json"
      provides: "npm-скрипт test:e2e"
      contains: "test:e2e"
  key_links:
    - from: "test/e2e.test.js"
      to: "infra/local-server.js"
      via: "child_process.spawn с env PORT=3101/STORE_ADAPTER=local/OUTPUT_DIR"
      pattern: "spawn\\("
    - from: "test/e2e.test.js"
      to: "GET /lines/:id/manifest + GET /lines/:id/steps/:step"
      via: "fetch poll loop по artifact-count и steps[stepId].error"
      pattern: "/manifest"
    - from: "test/e2e.test.js"
      to: "OUTPUT_DIR/{article}/{step}/v{N}/"
      via: "fs.statSync + ExcelJS.Workbook().xlsx.readFile для валидации D-02"
      pattern: "xlsx\\.readFile"
---

<objective>
Phase 5 — это ТЕСТ, а не фича. Цель: один автоматизированный `node:test` сценарий
(`test/e2e.test.js`), который через живой HTTP API (`infra/local-server.js`) прогоняет
реальный опросник лицевого молда + реальное фото через шаги 01→02→03→05→06 с настоящими
вызовами OpenAI (D-01) и доказывает, что в изолированном `OUTPUT_DIR` появился полный пакет
артефактов. Весь серверный код переиспользуется как есть — новый код только тонкий оркестратор
+ фикстура опросника + npm-скрипт.

Purpose: закрыть REL-02 — впервые доказать, что система работает как целое end-to-end без
ручного вмешательства. Закрывает последнее v1-требование и Phase 5 Success Criteria 1–3.

Output: `test/e2e.test.js`, `test/fixtures/e2e-face-questionnaire.json`, скрипт `test:e2e` в
`package.json`. Артефакты прогона остаются в `test/e2e-output/` для осмотра человеком.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-validation/05-CONTEXT.md
@.planning/phases/05-e2e-validation/05-RESEARCH.md
@.planning/phases/05-e2e-validation/05-PATTERNS.md
@.planning/phases/05-e2e-validation/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Эта фаза создаёт следующие НОВЫЕ символы/файлы (drift-проверка должна исключать их из
проверки на «незнакомые символы», т.к. они создаются здесь):

- `test/e2e.test.js` — новый файл (E2E-оркестратор)
- `test/fixtures/e2e-face-questionnaire.json` — новая фикстура
- `test/fixtures/e2e-face-mold.png` — новая фикстура (предоставляет пользователь, D-04)
- `test/e2e-output/` — новая директория артефактов (уже покрыта `.gitignore` через `test/`)
- npm-скрипт `test:e2e` в `package.json`
- Внутренние функции/хелперы внутри `test/e2e.test.js`: `startServer`, `waitForPort`,
  `pollStep`, `artifactPath`, `buildQuestionnaire` (или загрузка фикстуры), `preflight`
- Env-флаги, читаемые тестом: `RUN_E2E`, `E2E_BASE_URL` (опц.), `RUN_E2E_CLEAN` (опц.)

Всё серверное (api/index.js, step-*, local-server.js, versionStore.js) — READ-ONLY,
не модифицируется этой фазой.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Фикстура опросника + npm-скрипт test:e2e + skip-guard скелет</name>
  <files>test/fixtures/e2e-face-questionnaire.json, package.json, test/e2e.test.js</files>
  <read_first>
    - test/step-error-capture.test.js (buildQuestionnaire форма строки 26-48 — точная required-форма опросника, оба теста прошли с ней)
    - test/create-line.smoke.test.js (multipart event и проверка 200 + stepId 01-normalize)
    - layers/shared/config/template.master.json (moldTypes.face: topic/purpose/titleFull/annotation — подтверждает, что face-молду НЕ нужны доп. поля faceSize/poraType/faceOval; универсальной схемы moldType+sizes[] достаточно — это резолвит RESEARCH-предположение A1/Open Q2)
    - package.json (текущий scripts.test = "node --test 'test/**/*.test.js'")
    - input/questionnaire.schema.json (required-поля опросника)
  </read_first>
  <action>
    Создать `test/fixtures/e2e-face-questionnaire.json` — шаблон опросника лицевого молда по
    форме из step-error-capture.test.js buildQuestionnaire, но с `moldType` = `"face"` (D-03),
    осмысленными русскими значениями для face-молда (moldName напр. "Василиса", brand "ТопМолд",
    theme "Кукольное лицо", color "Телесный", priceBaseM 1200), полным массивом `sizes` из 5
    записей (XS/S/M/L/XL с moldSize/moldLength/moldWidth/moldHeight/moldWeight как в smoke-тесте),
    `photos: []` (заполнится сервером при multipart-загрузке). Поле `article` НЕ хардкодить
    в фикстуре финальным значением — оставить плейсхолдер или опустить, т.к. тест подставит
    уникальный article в рантайме (см. Task 2). НЕ добавлять faceSize/poraType/faceOval —
    подтверждено по template.master.json, что они не используются.

    В `package.json` добавить в `scripts` ключ `test:e2e` со значением
    `node --test test/e2e.test.js`. НЕ менять существующий `test`-скрипт.

    Создать `test/e2e.test.js` со ВСЕМ скелетом и skip-guard в самом начале: `'use strict'`,
    requires (node:test, node:assert, node:path, node:fs, node:net, node:child_process spawn,
    exceljs). Сразу после requires — skip-guard: если `!process.env.RUN_E2E`, зарегистрировать
    один `test('e2e (skipped — set RUN_E2E=1 to run)', { skip: true }, () => {})` и НЕ
    регистрировать реальный E2E-тест (return/ранний выход из регистрации). Это реализует D-06 +
    RESEARCH-рекомендацию A2: файл виден в `npm test`, но платный прогон идёт только при
    `RUN_E2E=1 npm run test:e2e`. Полное тело реального теста добавляется в Task 2 — здесь
    оставить TODO-маркер внутри ветки RUN_E2E.

    ВАЖНО (секреты, V7/V14): НЕ хардкодить OPENAI_API_KEY нигде в тесте/фикстуре. Ключ
    наследуется из окружения. `.env.local` и `test/e2e-output/` уже в `.gitignore` (через
    запись `test/`); новые файлы `test/e2e.test.js` и фикстуры потребуют `git add -f` при
    коммите (директория `test/` целиком gitignored, существующие тесты добавлены force) —
    отразить это в SUMMARY, сам git-add делает шаг коммита.
  </action>
  <verify>
    <automated>RUN_E2E= node --test test/e2e.test.js 2>&1 | grep -qiE 'skip|skipped' && node -e "const p=require('./package.json'); if(!p.scripts['test:e2e']) process.exit(1); JSON.parse(require('fs').readFileSync('test/fixtures/e2e-face-questionnaire.json','utf8'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/e2e-face-questionnaire.json` парсится как валидный JSON и содержит `"moldType": "face"` и массив `sizes` длиной 5
    - `package.json` scripts содержит `"test:e2e": "node --test test/e2e.test.js"` и сохраняет прежний `"test"`
    - `test/e2e.test.js` существует, начинается с `'use strict'`, содержит skip-guard на `process.env.RUN_E2E`
    - `node --test test/e2e.test.js` БЕЗ `RUN_E2E` выводит skipped (НЕ делает сетевых вызовов, exit 0)
    - В тесте/фикстуре нет литерала `OPENAI_API_KEY=` со значением (grep `sk-` возвращает 0 совпадений)
  </acceptance_criteria>
  <done>Фикстура опросника, npm-скрипт и skeleton-файл со skip-guard созданы; обычный node --test пропускает E2E без сетевых вызовов.</done>
</task>

<task type="auto">
  <name>Task 2: E2E-оркестратор — spawn server, pollStep, прогон pipeline, валидация артефактов</name>
  <files>test/e2e.test.js</files>
  <read_first>
    - test/e2e.test.js (скелет + skip-guard из Task 1 — текущее состояние)
    - infra/local-server.js (PORT/STORE_ADAPTER/OUTPUT_DIR/SHARED_LAYER_PATH из env; строка ~38 не перетирает заданные env при чтении .env.local → переданные через spawn имеют приоритет; listen на PORT)
    - functions/api/index.js (handleRegenerate ~строка 357: async-шаги 02-texts/03-images → respond(202,{queued,count}); sync 05-excel/06-assemble → 200; POST /lines → respond(200,{stepId:'01-normalize'}); handleGetStep → respond(200,{artifacts:[...]}); runLocally пишет steps[stepId].error при throw; force дефолтит true в regenerate)
    - frontend/PipelineApp.jsx (computeStepStatus ~строка 414-444 — семантика готовности: НЕТ поля status; готово=artifact-count, ВНИМАНИЕ legacy `*4` для images — НЕ копировать, для images готово=5 различных {size}_infographic.png т.к. IMAGE_TYPES=['infographic'])
    - functions/step-images/index.js (stub-фолбэк ~строка 225: при !OPENAI_API_KEY или ошибке Edits API пишется 1×1 PNG ~70 байт с needsReview:true и currentVersion всё равно растёт — Pitfall 2; валидация PNG должна требовать size > 1000)
    - functions/step-assemble/index.js (~строка 50-64: НЕ копирует файлы плоско — пишет assemble-report.json со ссылками + completedSteps)
    - functions/step-excel/index.js (FILE_NAMES: {article}_ozon.xlsx, {article}_wb.xlsx)
    - functions/step-texts/index.js (артефакты {size}_texts.json ×5)
    - test/create-line.smoke.test.js (форма POST /lines, проверка 200/stepId)
  </read_first>
  <action>
    Заполнить ветку `RUN_E2E` в `test/e2e.test.js` полным E2E-сценарием. Константы:
    `PORT = Number(process.env.E2E_PORT) || 3101` (отдельный порт, НЕ 3001 — конфликт с dev);
    `OUTPUT_DIR = path.join(__dirname, 'e2e-output')`; `SIZES = ['XS','S','M','L','XL']`;
    `SHARED = path.resolve(__dirname,'../layers/shared')`. Уникальный article в рантайме:
    `e2e-test-${YYYYMMDD-HHMMSS}` (например через new Date().toISOString с заменой нецифр) —
    критично против cache-hit по inputHash (Pitfall 5, D-05).

    Preflight (выполнять в начале теста, до spawn): если `!process.env.OPENAI_API_KEY` — fail
    с понятным сообщением "E2E требует реальный OPENAI_API_KEY (D-01); задай в .env.local или
    окружении". Если `test/fixtures/e2e-face-mold.png` не существует ИЛИ его size < 1000 байт —
    fail с сообщением "Положи реальное фото лицевого молда в test/fixtures/e2e-face-mold.png
    (D-04), текущая фикстура отсутствует/слишком мала". (1×1 заглушка ~70 байт провалит этот чек.)

    `startServer()`: `spawn('node', [path.resolve(__dirname,'../infra/local-server.js')], { env:
    { ...process.env, PORT:String(PORT), STORE_ADAPTER:'local', OUTPUT_DIR, SHARED_LAYER_PATH:
    SHARED }, stdio:'inherit' })`. Фолбэк D-07: если задан `process.env.E2E_BASE_URL` — НЕ
    спавнить, использовать его как baseUrl. `waitForPort(PORT, 10000)`: net.createConnection с
    ретраями каждые 200мс до connect или дедлайна. Гарантированный teardown: зарегистрировать
    через `test.after(...)` или try/finally — `server.kill('SIGTERM')` ТОЛЬКО после всех
    проверок (Pitfall 4: фоновые задачи живут в процессе сервера, не убивать раньше времени).

    Прогон по D-08 через `fetch` к `baseUrl` (`http://127.0.0.1:${PORT}` или E2E_BASE_URL):
    (1) POST /lines с FormData: `fd.append('questionnaire', JSON.stringify(q))` где q —
    фикстура с подставленным уникальным article; `fd.append('photo', new Blob([buf],{type:
    'image/png'}), 'e2e-face-mold.png')`; БЕЗ ручного Content-Type. Ассертить статус 200 (НЕ 201
    — api возвращает 200) и body.stepId === '01-normalize'. 01-normalize уже выполнен внутри POST
    /lines — отдельно его НЕ дёргать.
    (2) POST /lines/:article/steps/02-texts/regenerate (тело {} — force дефолтит true) → ассертить
    202 → pollStep('02-texts', {expect:5, timeoutMs:5*60*1000}).
    (3) POST .../03-images/regenerate → 202 → pollStep('03-images', {expect:5, timeoutMs:10*60*1000}).
    (4) POST .../05-excel/regenerate → ассертить 200.
    (5) POST .../06-assemble/regenerate → ассертить 200.

    `pollStep(baseUrl, article, stepId, {expect, timeoutMs})`: цикл до дедлайна — GET
    /lines/:article/manifest → meta = manifest.steps[stepId]; если `meta?.error` непустой —
    throw сразу с error+failedAt (Pitfall 3, проверять ПЕРЕД артефактами); если
    `meta?.currentVersion` — GET /lines/:article/steps/:stepId → `(step.artifacts||[]).length`;
    console.log `[e2e] ${stepId}: ${n}/${expect} артефактов...`; если n >= expect — return
    manifest. Пауза 5000мс между опросами. По дедлайну — throw "timed out". НЕ опираться на
    несуществующее поле `status`.

    Валидация артефактов (D-02), версии брать из `manifest.steps[stepId].currentVersion` после
    polling (НЕ хардкодить). `artifactPath(stepId,version,name) = path.join(OUTPUT_DIR, article,
    stepId, 'v'+version, name)`:
    - master-data.json существует и парсится в 5 записей (01-normalize currentVersion).
    - Для каждого size: {size}_texts.json существует; `!/\{\{.*?\}\}/.test(content)` — нет
      нераскрытых плейсхолдеров.
    - Для каждого size: {size}_infographic.png существует и `fs.statSync(...).size > 1000`
      (Pitfall 2 — отсеять 1×1 stub). Дополнительно: если в 03-images history любой size имеет
      needsReview === true — console.warn (не обязательно fail, но залогировать).
    - {article}_ozon.xlsx и {article}_wb.xlsx: `await new ExcelJS.Workbook().xlsx.readFile(path)`
      (throws при повреждении) + assert worksheets.length > 0.
    - 06-assemble: assemble-report.json существует, парсится, содержит completedSteps.
    В конце — распечатать список созданных артефактов и их размеры (specifics: stdout-прогресс).

    Cleanup (discretion, RESEARCH Q3): НЕ чистить e2e-output автоматически по умолчанию
    (оставить для осмотра человеком — это и есть «получил готовый пакет»); опционально удалять
    при `process.env.RUN_E2E_CLEAN`.

    Таймаут самого node:test-теста выставить с запасом (напр. опция `{ timeout: 20*60*1000 }`
    на test()), чтобы 5+10 мин polling уложились.
  </action>
  <verify>
    <automated>RUN_E2E= node --test test/e2e.test.js 2>&1 | grep -qiE 'skip' && node -e "const s=require('fs').readFileSync('test/e2e.test.js','utf8'); for(const t of ['pollStep','spawn','waitForPort','xlsx.readFile','03-images','steps[','currentVersion']) if(!s.includes(t)){console.error('MISSING',t);process.exit(1)} if(/status\s*===\s*['\"]done/.test(s)){console.error('uses nonexistent status field');process.exit(1)} console.log('STRUCTURE OK')"</automated>
    <human-check>Phase gate (платный, выполняется ОДИН раз перед /gsd-verify-work): пользователь кладёт реальное фото в test/fixtures/e2e-face-mold.png, экспортирует OPENAI_API_KEY (через .env.local), запускает `RUN_E2E=1 npm run test:e2e`. Ожидаемо: тест проходит exit 0; в test/e2e-output/{article}/ лежат master-data.json, 5 текстов, 5 PNG (> 1000 байт каждый), 2 xlsx, assemble-report.json; в stdout виден прогресс polling и финальный список артефактов.</human-check>
  </verify>
  <acceptance_criteria>
    - `test/e2e.test.js` содержит функции `pollStep`, `waitForPort`, использует `spawn(` для запуска infra/local-server.js
    - `pollStep` проверяет `steps[stepId].error` ПЕРЕД проверкой artifact-count и считает готовность по `(step.artifacts||[]).length >= expect`, НЕ по полю `status` (grep `status === 'done'` → 0 совпадений)
    - Для 03-images `expect` = 5 (НЕ 20 — IMAGE_TYPES=['infographic'])
    - Валидация PNG требует `size > 1000`, тексты проверяются на отсутствие `{{...}}`, xlsx через `ExcelJS ... xlsx.readFile`
    - Preflight падает с понятным сообщением при отсутствии OPENAI_API_KEY или отсутствии/малом размере фото-фикстуры
    - Сервер убивается в teardown (`server.kill`) только после всех проверок
    - `RUN_E2E= node --test test/e2e.test.js` (без флага) по-прежнему skip, без сетевых вызовов, exit 0
    - Behavior (phase gate, human-check): `RUN_E2E=1 npm run test:e2e` с реальным ключом и реальным фото завершается exit 0 и производит полный пакет артефактов в test/e2e-output/{article}/
  </acceptance_criteria>
  <done>E2E-оркестратор полностью реализован: поднимает сервер, прогоняет 01→06, поллит async-шаги по artifact-count, валидирует все артефакты по D-02. Без RUN_E2E — skip; с RUN_E2E=1 + реальными ключами/фото — зелёный прогон с полным пакетом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → spawned local-server (env) | Секреты (OPENAI_API_KEY) пересекают границу процесса через spawn env |
| test fixture → POST /lines (multipart) | Фото и questionnaire-JSON пересекают границу HTTP в живой API |
| filesystem (OUTPUT_DIR) → git | Артефакты прогона и секреты не должны попасть в репозиторий |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | OPENAI_API_KEY в тесте/фикстуре/логах | mitigate | Ключ только из env (наследуется spawn); НЕ хардкодить; НЕ логировать значение; acceptance-критерий grep `sk-`→0; `.env.local` уже в `.gitignore` |
| T-05-02 | Tampering | Path traversal через имя загружаемого фото | accept | Уже митигировано в api/index.js (basename+sanitize, isImageBuffer magic-bytes); E2E использует безопасное имя `e2e-face-mold.png` |
| T-05-03 | Denial of Service (local) | Orphan-процесс сервера держит порт 3101 после теста | mitigate | Гарантированный `server.kill('SIGTERM')` в teardown + wait-for-port перед стартом; отдельный порт 3101 (не 3001) |
| T-05-04 | Information Disclosure | Артефакты прогона (тексты/картинки/секреты) утекают в git | mitigate | `test/e2e-output/` уже покрыт `.gitignore` (запись `test/`); новые файлы добавляются явным `git add -f` только для test/e2e.test.js + фикстуры опросника, НЕ для output |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Фаза НЕ устанавливает новых пакетов — только встроенные модули Node + уже присутствующий exceljs (RESEARCH Package Legitimacy Audit: аудит не требуется) |
</threat_model>

<verification>
- `RUN_E2E= node --test test/e2e.test.js` → skipped, exit 0, без сетевых вызовов
- `npm test` (вся unit-suite) остаётся зелёной, E2E не запускается
- Структурные grep-гейты Task 1/2 проходят
- Phase gate (один раз, платный): `RUN_E2E=1 npm run test:e2e` с реальным OPENAI_API_KEY и
  реальным фото в test/fixtures/e2e-face-mold.png → exit 0 + полный пакет в test/e2e-output/
</verification>

<success_criteria>
- REL-02: реальный опросник + фото проходит шаги 01–05(06) без ручного вмешательства (один npm-скрипт)
- SC-1: POST /lines + последовательный запуск шагов автоматом, без ручных шагов между ними
- SC-2: после 06-assemble на диске master-data.json + 5 текстов + 5 PNG (>1000б) + 2 xlsx + assemble-report.json
- SC-3: каждый шаг завершается «готов» (artifact-count) или «упал» (error) — ни один не висит сверх таймаута
- Платный E2E изолирован за RUN_E2E-флагом — не идёт на каждом npm test
</success_criteria>

<output>
Создать `.planning/phases/05-e2e-validation/05-01-SUMMARY.md` по завершении.
Отметить в SUMMARY: для коммита `test/e2e.test.js` и `test/fixtures/e2e-face-questionnaire.json`
нужен `git add -f` (директория `test/` целиком в `.gitignore`, как и существующие тест-файлы);
`test/e2e-output/` и `test/fixtures/e2e-face-mold.png` НЕ коммитить (артефакты прогона / большой
бинарь / возможные секреты).
</output>
