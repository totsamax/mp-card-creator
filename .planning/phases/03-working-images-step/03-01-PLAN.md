---
phase: 03-working-images-step
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/step-images.test.js
  - test/fixtures/infographic.png
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03, IMG-04]

must_haves:
  truths:
    - "Running `node --test 'test/step-images.test.js'` exits non-zero (suite is RED) against current step-images code"
    - "The test suite encodes the full Phase 3 contract: 5-size slide generation, prompt has no unresolved tokens, photo used as reference, attempts[] accumulation, 400 on missing photo, 202 from regenerate route"
    - "npm test (full suite) still discovers the new file via the existing glob"
  artifacts:
    - path: "test/step-images.test.js"
      provides: "RED contract for step-images covering IMG-01..04 + D-09/D-10/D-11"
      min_lines: 120
    - path: "test/fixtures/infographic.png"
      provides: "Background template fixture for real-generation-path tests (stub path does not need it)"
  key_links:
    - from: "test/step-images.test.js"
      to: "functions/step-images/index.js"
      via: "require + handler invocation"
      pattern: "require\\(.*step-images"
    - from: "test/step-images.test.js"
      to: "functions/api/index.js"
      via: "createLine seeds master-data + photo, regenerate route returns 202"
      pattern: "require\\(.*api/index"
---

<objective>
Create the failing RED test contract (`test/step-images.test.js`) that encodes every Phase 3 success criterion before any production code changes. The suite must FAIL against the current broken `functions/step-images/index.js` (which calls `/v1/images/generations`, has the `{{faceSize}}` bug, a no-op `enqueueRetry`, a broken `buildAttemptsLog`, and no `buildEditRequest` export).

Purpose: Lock the behavioral contract for the step-images rewrite so Plan 03-02 has a precise, automated target and Plan 03-03 can prove GREEN.
Output: `test/step-images.test.js` (RED), `test/fixtures/infographic.png` placeholder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-working-images-step/03-RESEARCH.md
@.planning/phases/03-working-images-step/03-PATTERNS.md
@test/step-texts.test.js
@functions/step-images/index.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add infographic.png background fixture</name>
  <files>test/fixtures/infographic.png</files>
  <read_first>
    - test/fixtures/test-mold.png (existing photo fixture — confirms the fixtures directory and PNG conventions)
    - .planning/phases/03-working-images-step/03-RESEARCH.md (lines 267-282, 408-410: templates dir does not exist; stub-mode tests do NOT read the template; a placeholder is only needed for any future real-API path)
  </read_first>
  <action>
    Create a minimal valid PNG at test/fixtures/infographic.png so that any test exercising the real-generation path (if added) can resolve a background template under SHARED_LAYER_PATH/templates. Generate it by writing the 1x1 transparent PNG bytes decoded from the base64 string already used as the stub in functions/step-images/index.js (the constant beginning `iVBORw0KGgoAAAANSUhEUgAAAAEAAAAB`). Use the Write tool is not suitable for binary; instead generate via a one-line Node command: decode that exact base64 to a Buffer and fs.writeFileSync to test/fixtures/infographic.png. This fixture is a placeholder only; stub-mode tests (OPENAI_API_KEY unset) return before reading any template per D-04, so the suite does not depend on this file for RED/GREEN.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('test/fixtures/infographic.png');process.exit(b.length>0 && b.slice(0,8).toString('hex')==='89504e470d0a1a0a'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - test/fixtures/infographic.png exists and its first 8 bytes are the PNG magic number 89 50 4E 47 0D 0A 1A 0A
    - File size > 0 bytes
  </acceptance_criteria>
  <done>A valid placeholder PNG exists at test/fixtures/infographic.png.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED contract test/step-images.test.js</name>
  <files>test/step-images.test.js</files>
  <read_first>
    - test/step-texts.test.js (THE analog — copy env preamble lines 7-10, createLine lines 25-69 VERBATIM, readManifest lines 86-89 VERBATIM; adapt runTexts lines 75-81 into runImages by adding imageType)
    - functions/step-images/index.js (current handler — confirms it does NOT export buildEditRequest, uses {{faceSize}}, calls enqueueRetry; these are what the tests must fail against)
    - functions/api/index.js (lines 369-392: dispatch loop + 202 response for /steps/03-images/regenerate; line 12 IMAGE_TYPES; lines 126-129 + 303 artifact route)
    - layers/shared/config/prompts.images.json (current infographic prompt contains {{faceSize}} — regression guard target)
    - .planning/phases/03-working-images-step/03-PATTERNS.md (lines 127-156: exact test harness mapping and which step-texts lines to copy)
  </read_first>
  <action>
    Create test/step-images.test.js modeled on test/step-texts.test.js. Env preamble: set STORE_ADAPTER='local', OUTPUT_DIR=path.join(__dirname,'tmp-output'), SHARED_LAYER_PATH=path.join(__dirname,'../layers/shared'), and explicitly delete process.env.OPENAI_API_KEY (do NOT set it) so the stub 1x1 PNG path runs and no network/template read occurs (D-04, RESEARCH Pitfall 3). Require the api handler from ../functions/api/index.js and the step module as `const stepImages = require('../functions/step-images/index.js'); const { handler } = stepImages;`.

    Copy createLine and readManifest VERBATIM from step-texts.test.js (createLine seeds master-data AND uploads test-mold.png via store.putArtifact(article,'photos',1,...) — REQUIRED so the photo exists for IMG-03/D-03). Add a helper runImages(article, size, imageType='infographic', extra={}) that calls handler({ body: JSON.stringify({ article, size, imageType, attempt:1, force:true, ...extra }) }) and parses the body — always force:true (Pitfall 5), unique article per test.

    Write these test cases (all RED against current code):
    1. IMG-02/03/04 "all 5 sizes produce {size}_infographic.png": createLine unique article, loop SIZES=['XS','S','M','L','XL'], runImages each, assert statusCode===200 and body.artifactName===`${size}_infographic.png`, and assert the manifest steps['03-images'].history contains an entry per size (via readManifest). RED reason: current generateImage signature is (sizeRecord,imageType,feedback) — passes today only by luck of stub; the firm RED assertions below break current code.
    2. D-09 "prompt has no unresolved {{...}} and never references faceSize": assert typeof stepImages.buildEditRequest === 'function' (NOT exported today → RED). Call stepImages.buildEditRequest(article, sizeRecord, 'infographic', []) where sizeRecord is taken from the seeded master-data (read via readManifest path or reconstruct from createLine inputs), assert returned .prompt is a string, !/\{\{[^}]+\}\}/.test(prompt), and !prompt.includes('faceSize') and !prompt.includes('undefined').
    3. IMG-03 "photo used as reference": from the same buildEditRequest result assert .imageCount >= 2 (1 background + >=1 mold photo) — proving the photo buffers are included in image[]. RED: buildEditRequest does not exist.
    4. D-10/D-11 "attempts[] accumulates and recursion terminates": this requires a critic that can reject. Since runCritic is a stub returning ok:true (D-04, unchanged in Phase 3), assert the SHAPE contract instead: after a successful run, readManifest's last history entry for '03-images' has an `attempts` array whose every element has a numeric `attempt` and an object `criticVerdict`, and attempts[0].attempt === 1. RED reason: current buildAttemptsLog reads from stepMeta.history (wrong source) so on a fresh article attempts is [{attempt:1,...}] but the field threading is broken when recursion would occur; assert attempts is a non-empty array of correctly-shaped entries — this guards D-11's `[...attemptsLog,{attempt,criticVerdict}]` accumulator.
    5. D-03 "400 when no mold photo": seed a line WITHOUT a photo. Since createLine always uploads a photo, add a second helper createLineNoPhoto(article,moldType) that posts the same questionnaire but with files:[] (empty) so no photo artifact is written. Then runImages on it and assert statusCode===400 and body.error includes 'no mold photo found'. RED: current code throws inside generateImage → surfaces as 500, not 400.
    6. IMG-01 "regenerate route returns 202": call the api handler directly with httpMethod:'POST', path:`/lines/${article}/steps/03-images/regenerate`, body JSON.stringify({force:true}); assert result.statusCode===202. Use an article seeded by createLine. (This may pass today; keep it as the route-contract guard.)

    Do not set OPENAI_API_KEY anywhere. Do not import @aws-sdk/client-sqs.
  </action>
  <verify>
    <automated>node --test 'test/step-images.test.js'; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `node --test 'test/step-images.test.js'` exits NON-ZERO (suite is RED)
    - The failure includes at least the assertion `typeof stepImages.buildEditRequest === 'function'` failing (buildEditRequest not yet exported)
    - The file defines test cases covering: 5-size generation, no unresolved tokens / no 'faceSize', photo-as-reference (imageCount>=2), attempts[] shape, 400 on missing photo, 202 from regenerate route
    - The file sets STORE_ADAPTER='local' and does NOT set process.env.OPENAI_API_KEY
    - `grep -c "@aws-sdk/client-sqs" test/step-images.test.js` returns 0
  </acceptance_criteria>
  <done>A RED test suite encoding the full Phase 3 contract exists and fails against the current step-images code.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-01)

New files:
- `test/step-images.test.js` — RED contract suite (test cases listed above)
- `test/fixtures/infographic.png` — placeholder background PNG

New test helpers (inside test/step-images.test.js):
- `createLine(article, moldType)` — copied verbatim from step-texts.test.js
- `createLineNoPhoto(article, moldType)` — variant with empty files[] (no photo upload)
- `runImages(article, size, imageType, extra)` — invokes step-images handler with force:true
- `readManifest(article)` — copied verbatim from step-texts.test.js

Symbols this plan EXPECTS Plan 03-02 to create (asserted-against, not created here):
- `exports.buildEditRequest(article, sizeRecord, imageType, feedback)` → `{ prompt, imageCount }` in functions/step-images/index.js
</artifacts_produced>

<verification>
- `node --test 'test/step-images.test.js'` exits non-zero (RED)
- `npm test` still runs and discovers step-images.test.js (full suite will be RED until Plan 03-02/03)
- No new npm packages added (package.json dependencies unchanged)
</verification>

<success_criteria>
- RED contract committed; failures are due to missing buildEditRequest export + 400/recursion contract, not syntax errors
- test/fixtures/infographic.png is a valid PNG
</success_criteria>

<output>
Create `.planning/phases/03-working-images-step/03-01-SUMMARY.md` when done
</output>
