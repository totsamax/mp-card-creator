---
phase: 03-working-images-step
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - test/step-images.test.js
autonomous: false
requirements: [IMG-01, IMG-04]

must_haves:
  truths:
    - "Full suite (npm test) is GREEN — Phase 2's 10 tests plus the new step-images tests all pass"
    - "POST /lines/:id/steps/03-images/regenerate returns 202 (IMG-01, verified via api handler test)"
    - "Generated slides are retrievable via GET /lines/:id/steps/03-images/artifacts/:name with Content-Type image/png (IMG-04)"
    - "The existing UI 'Запустить шаг'/'Перегенерировать' button triggers generation and slides appear in ImagesView (IMG-01 + IMG-04, human-verified)"
  artifacts:
    - path: "test/step-images.test.js"
      provides: "Adds IMG-04 artifact-retrieval assertion (GET artifacts returns the saved PNG)"
      contains: "artifacts"
  key_links:
    - from: "test/step-images.test.js"
      to: "functions/api/index.js handleGetArtifact"
      via: "GET /steps/03-images/artifacts/:name returns base64 PNG"
      pattern: "artifacts"
    - from: "frontend/PipelineApp.jsx VersionPicker"
      to: "POST /lines/:id/steps/03-images/regenerate"
      via: "handleRegenerateStep (already exists)"
      pattern: "regenerate"
---

<objective>
Close the user-facing loop: prove the existing UI trigger produces 202, the generated slides are retrievable through the artifacts API (IMG-04), and the full test suite is GREEN. The trigger button already exists (RESEARCH: `VersionPicker` + `handleRegenerateStep` already POST to `/lines/:id/steps/03-images/regenerate`), so IMG-01 is a verification task, not new wiring.

This plan adds the IMG-04 artifact-retrieval test, runs the full suite, and ends with a human-verify checkpoint confirming the real button → slide flow in the UI.

Purpose: Satisfy IMG-01 (button → 202) and IMG-04 (slides saved + visible in UI) with automated + human proof.
Output: Extended test coverage for artifact retrieval; GREEN full suite; human-verified UI flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-working-images-step/03-RESEARCH.md
@.planning/phases/03-working-images-step/03-PATTERNS.md
@test/step-images.test.js
@functions/api/index.js
@frontend/PipelineApp.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IMG-04 artifact-retrieval test + run full suite GREEN</name>
  <files>test/step-images.test.js</files>
  <behavior>
    - After running step-images for a size, GET /lines/:id/steps/03-images/artifacts/{size}_infographic.png via the api handler returns statusCode 200 with Content-Type image/png (IMG-04)
    - The full suite (npm test) is GREEN including Phase 2's existing tests
  </behavior>
  <read_first>
    - test/step-images.test.js (the suite from Plan 03-01, now GREEN after 03-02 — add to it; reuse createLine/runImages/readManifest helpers)
    - functions/api/index.js (lines 126-129 artifact route regex `/^\/steps\/([^/]+)\/artifacts\/([^/]+)$/`; lines 303-341 handleGetArtifact returning base64 PNG with contentTypes map; png → 'image/png')
    - .planning/phases/03-working-images-step/03-RESEARCH.md (lines 336-338 artifact retrieval path; lines 393-401 Test Map for IMG-01/IMG-04)
  </read_first>
  <action>
    Add a test case to test/step-images.test.js: "IMG-04: generated slide is retrievable via GET artifacts". Steps: createLine a unique article (seeds master-data + photo), runImages(article,'M','infographic') and assert 200 + body.version is a number, then call the api handler with httpMethod:'GET', path:`/lines/${article}/steps/03-images/artifacts/M_infographic.png`, queryStringParameters:{ version: String(body.version) }. Assert result.statusCode===200 and the response headers Content-Type === 'image/png'. (handleGetArtifact returns base64 image content with the png content-type from the contentTypes map.)

    Also add (or confirm from Plan 03-01) the IMG-01 route test: api handler POST `/lines/${article}/steps/03-images/regenerate` with body JSON.stringify({force:true}) returns statusCode 202.

    Run the FULL suite and confirm GREEN. Do not modify production code in this task — if a test reveals a defect, the fix belongs in Plan 03-02's files (flag it), not here.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits ZERO (full suite GREEN, including Phase 2's 10 tests)
    - test/step-images.test.js contains an IMG-04 case asserting the artifacts GET returns Content-Type 'image/png' and statusCode 200
    - test/step-images.test.js contains an IMG-01 case asserting POST .../03-images/regenerate returns 202
    - No production source files (functions/, layers/) modified by this task
  </acceptance_criteria>
  <done>Artifact retrieval (IMG-04) and the 202 route (IMG-01) are covered by GREEN automated tests, and the full suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify UI button triggers generation and slides appear</name>
  <action>Pause for the user to manually verify the end-to-end UI flow described in <how-to-verify>. No code is written in this task; the executor presents the verification steps and waits for the resume-signal.</action>
  <what-built>
    step-images now composites the mold photo onto the infographic background via the OpenAI Edits API, the recursive retry loop works locally, and slides are saved + served through the artifacts API. The UI 'Запустить шаг'/'Перегенерировать' button in the Изображения (03-images) step already POSTs to the regenerate route. To generate REAL (non-stub) slides, the user must supply a background template and an OpenAI key — otherwise the stub 1x1 PNG path runs.
  </what-built>
  <how-to-verify>
    Optional (real slides): place a background PNG at layers/shared/templates/infographic.png and set OPENAI_API_KEY in .env.local. (Without these, generation uses the stub 1x1 PNG — the flow still works end-to-end, slides just appear as 1x1.)
    1. Run `npm run dev` (API on :3001, Vite on :5173).
    2. Open http://localhost:5173, create or open a line that has a mold photo uploaded.
    3. Go to the «Изображения» step. Click «Запустить шаг» (or «Перегенерировать»).
    4. Confirm the request returns 202 (Network tab: POST /lines/:id/steps/03-images/regenerate → 202).
    5. After the background run completes (a few seconds in stub mode), reload/poll the step; confirm output/{article}/03-images/v{N}/ contains {size}_infographic.png for all 5 sizes (XS–XL), and the ImagesView shows the infographic tile populated for each size.
    6. Confirm GET /lines/:id/steps/03-images/artifacts/M_infographic.png?version=N returns the PNG (image renders, not a 404).
  </how-to-verify>
  <resume-signal>Type "approved" if the button triggers 202 and slides appear/retrieve, or describe what failed.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-03)

Modified files:
- `test/step-images.test.js` — adds IMG-04 artifact-retrieval test + IMG-01 202-route test (if not already present from Plan 03-01)

No new production symbols. This plan is verification + human confirmation of IMG-01/IMG-04.
</artifacts_produced>

<verification>
- `npm test` exits zero (full suite GREEN)
- IMG-04 artifact GET returns image/png 200
- IMG-01 regenerate route returns 202
- Human checkpoint confirms UI button → 202 → slides visible/retrievable
- No new npm packages (package.json dependencies unchanged)
</verification>

<success_criteria>
- IMG-01: pressing the UI button POSTs to /steps/03-images/regenerate and gets 202 (automated + human-verified)
- IMG-04: slides are saved to output/ and retrievable via the artifacts API / visible in UI (automated + human-verified)
- Full test suite GREEN
</success_criteria>

<output>
Create `.planning/phases/03-working-images-step/03-03-SUMMARY.md` when done
</output>
