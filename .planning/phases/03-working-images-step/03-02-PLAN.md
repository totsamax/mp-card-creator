---
phase: 03-working-images-step
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - functions/step-images/index.js
  - layers/shared/config/prompts.images.json
  - functions/api/index.js
autonomous: true
requirements: [IMG-02, IMG-03]

must_haves:
  truths:
    - "step-images calls OpenAI /v1/images/edits with image[] = [background, ...mold photos] (IMG-02, IMG-03, D-01)"
    - "generateImage substitution chain uses {{moldSize}} not {{faceSize}}, and includes {{topic}}/{{purpose}} (D-09, D-12)"
    - "Critic rejection recurses via exports.handler self-call threading attemptsLog (D-10), and attempts[] = [...attemptsLog, {attempt, criticVerdict}] (D-11)"
    - "buildEditRequest is exported and returns {prompt, imageCount} with no unresolved tokens"
    - "IMAGE_TYPES in functions/api/index.js is ['infographic'] → exactly 5 dispatch messages (D-05)"
    - "Background template read from layers/shared/templates/{imageType}.png; missing → 500 with friendly message (D-00c)"
    - "No mold photo → handler returns 400 'no mold photo found' (D-03)"
  artifacts:
    - path: "functions/step-images/index.js"
      provides: "Rewritten image-in-image compositor: Edits API, recursive retry, buildEditRequest export"
      contains: "v1/images/edits"
    - path: "layers/shared/config/prompts.images.json"
      provides: "infographic composition instruction with {{moldSize}}/{{topic}}/{{purpose}}; no {{faceSize}}"
      contains: "{{topic}}"
    - path: "functions/api/index.js"
      provides: "IMAGE_TYPES single-type constant"
      contains: "const IMAGE_TYPES = ['infographic']"
  key_links:
    - from: "functions/step-images/index.js"
      to: "https://api.openai.com/v1/images/edits"
      via: "fetch POST with FormData body (no manual Content-Type)"
      pattern: "v1/images/edits"
    - from: "functions/step-images/index.js"
      to: "layers/shared/templates/{imageType}.png"
      via: "fs.readFileSync(path.join(SHARED,'templates',...))"
      pattern: "templates"
    - from: "functions/step-images/index.js"
      to: "store.listArtifacts / store.getArtifact ('photos', 1)"
      via: "read mold photos as Buffers for image[]"
      pattern: "listArtifacts\\(.*photos"
    - from: "functions/step-images/index.js"
      to: "exports.handler (self)"
      via: "recursive retry threading attemptsLog (D-10)"
      pattern: "exports\\.handler\\("
---

<objective>
Rewrite `functions/step-images/index.js` from a broken text-to-image generator into a working image-in-image compositor, mirroring the already-fixed `functions/step-texts/index.js` pattern. Fix the four carry-forward bugs (D-09 faceSize→moldSize, D-10 recursive retry, D-11 attemptsLog accumulation, D-01 Edits API), add D-12 topic/purpose tokens, change `IMAGE_TYPES` to `['infographic']` (D-05), and rework the infographic prompt into a composition instruction.

This is the core vertical slice: after this plan, pressing the existing UI button generates a real infographic slide per size that composites the mold photo onto the background template.

Purpose: Make IMG-02 (slides generated from fixed template) and IMG-03 (mold photo used as reference) real.
Output: Rewritten step-images handler with `buildEditRequest` export, updated prompts.images.json, single-type IMAGE_TYPES.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-working-images-step/03-RESEARCH.md
@.planning/phases/03-working-images-step/03-PATTERNS.md
@functions/step-images/index.js
@functions/step-texts/index.js
@layers/shared/config/prompts.images.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite generateImage as Edits-API compositor + export buildEditRequest</name>
  <files>functions/step-images/index.js</files>
  <behavior>
    - buildEditRequest(article, sizeRecord, imageType, feedback) returns { prompt, imageCount } where prompt has all tokens resolved (no {{...}}, no 'faceSize', no 'undefined') and imageCount = 1 (background) + number of mold photos
    - generateImage with OPENAI_API_KEY unset returns the 1x1 transparent PNG stub Buffer (unchanged) before any template/photo read
    - generateImage with a key set reads the background template, reads all mold photos, builds FormData with image[]=[bg,...photos], POSTs /v1/images/edits, returns Buffer from data.data[0].b64_json
    - Missing background template → throws "Background template not found: ..." (surfaces as 500)
  </behavior>
  <read_first>
    - functions/step-images/index.js (FULL — current generateImage lines 108-142, imports lines 1-13, call site line 53)
    - functions/step-texts/index.js (analog: generateTexts structure, prompt substitution lines ~108-119 using {{moldSize}}/{{topic}}/{{purpose}}, runCritic export at line 237 `exports.runCritic`)
    - .planning/phases/03-working-images-step/03-RESEARCH.md (lines 99-180: the exact generateImage rewrite pattern, FormData/Blob, image[] order, NO manual Content-Type, b64_json parsing; lines 176-181 gotchas)
    - .planning/phases/03-working-images-step/03-PATTERNS.md (lines 26-35 imports incl. add `fs`; lines 69-75 generateImage rewrite + buildEditRequest export guidance)
  </read_first>
  <action>
    Add `const fs = require('fs');` to the imports (step-images currently imports only crypto + path). Reuse the existing SHARED constant (line 6).

    Extract a NEW exported pure-ish helper buildEditRequest(article, sizeRecord, imageType, feedback) that:
    - builds the prompt from promptsTmpl.prompts[imageType] || promptsTmpl.prompts.infographic, substituting these EXACT sizeRecord fields: {{moldName}}→moldName, {{moldSize}}→moldSize (D-09, replacing the old {{faceSize}}→faceSize), {{color}}→color, {{moldLength}}→moldLength, {{moldWidth}}→moldWidth, {{moldHeight}}→moldHeight, {{toyFrom}}→toyFrom, {{toyTo}}→toyTo, {{topic}}→topic (D-12), {{purpose}}→purpose (D-12);
    - appends promptsTmpl.feedbackSuffix.replace('{{issues}}', feedback.join('; ')) when feedback.length > 0;
    - reads photo names via `await store.listArtifacts(article, 'photos', 1)`;
    - returns { prompt, imageCount: 1 + (photoNames ? photoNames.length : 0) }.
    Export it: `exports.buildEditRequest = buildEditRequest;` (mirror step-texts exporting runCritic). Note: buildEditRequest is async because listArtifacts is async — the test awaits it.

    Rewrite generateImage(article, sizeRecord, imageType, feedback) [NOTE new first arg `article`]:
    - compute apiKey = process.env.OPENAI_API_KEY;
    - KEEP the stub branch VERBATIM (D-04): if (!apiKey) return Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==','base64'); — this returns BEFORE any template/photo read;
    - build the prompt via the same substitution chain used in buildEditRequest (reuse the helper or duplicate the chain; prompt string must be identical);
    - read background: templatePath = path.join(SHARED, 'templates', `${imageType}.png`); wrap fs.readFileSync(templatePath) in try/catch → on failure throw new Error(`Background template not found: ${templatePath}. Add ${imageType}.png to layers/shared/templates/`);
    - read all mold photos: photoNames = await store.listArtifacts(article,'photos',1); for each name push await store.getArtifact(article,'photos',1,name) into photoBuffers (do NOT throw here for empty — the 400 check lives in the handler, Task added below);
    - build const form = new FormData(); form.append('model','gpt-image-1'); form.append('prompt',prompt); form.append('size','1024x1024'); form.append('image[]', new Blob([bgBuffer],{type:'image/png'}), `${imageType}.png`); photoBuffers.forEach((buf,i)=>form.append('image[]', new Blob([buf],{type:'image/png'}), `mold-${i}.png`));
    - fetch('https://api.openai.com/v1/images/edits', { method:'POST', headers:{ Authorization:`Bearer ${apiKey}` }, body: form }) — DO NOT set Content-Type (undici derives the multipart boundary, per STATE.md decision + RESEARCH Pitfall 2);
    - if (!res.ok) throw new Error(`OpenAI Images Edits API error: ${res.status} ${await res.text()}`); const data = await res.json(); return Buffer.from(data.data[0].b64_json,'base64').

    Update the generateImage call site (currently line 53 `generateImage(sizeRecord, imageType, feedback)`) to pass article first: `generateImage(article, sizeRecord, imageType, feedback)`.

    FormData and Blob are Node 18+ globals — do NOT require them.
  </action>
  <verify>
    <automated>node -e "const m=require('./functions/step-images/index.js'); if(typeof m.buildEditRequest!=='function')process.exit(1); const s=require('fs').readFileSync('functions/step-images/index.js','utf8'); process.exit(/v1\/images\/edits/.test(s)&&!/v1\/images\/generations/.test(s)&&!/faceSize/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `exports.buildEditRequest` is a function
    - functions/step-images/index.js contains the string `v1/images/edits` and does NOT contain `v1/images/generations`
    - functions/step-images/index.js does NOT contain the substring `faceSize` (D-09)
    - functions/step-images/index.js contains `{{topic}}` and `{{purpose}}` substitution calls (D-12)
    - The fetch call to /v1/images/edits sets only an Authorization header (no `'Content-Type'` key on that request's headers)
    - generateImage's first parameter is `article`; the call site passes `article` as the first argument
    - `const fs = require('fs');` present in imports
  </acceptance_criteria>
  <done>generateImage composites background + photos via the Edits API, buildEditRequest is exported, and faceSize is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recursive retry, attemptsLog accumulation, and 400 missing-photo guard</name>
  <files>functions/step-images/index.js</files>
  <behavior>
    - msg destructure includes attemptsLog = []
    - terminal save branch sets historyEntry.attempts = [...attemptsLog, { attempt, criticVerdict }]
    - critic-reject branch recurses via exports.handler with attempt+1 and attemptsLog threaded forward (no YMQ)
    - handler returns 400 {error:'no mold photo found'} when store.listArtifacts(article,'photos',1) is empty, BEFORE calling generateImage
    - enqueueRetry and buildAttemptsLog helpers are deleted; no @aws-sdk/client-sqs usage remains in this file
  </behavior>
  <read_first>
    - functions/step-images/index.js (current handler lines 21-102, enqueueRetry lines 194-209, buildAttemptsLog lines 227-230, destructure line 25, attempts field line 83)
    - functions/step-texts/index.js (analog: destructure line 26 with attemptsLog=[], attempts accumulation line 78, recursion lines 89-93)
    - .planning/phases/03-working-images-step/03-PATTERNS.md (lines 37-77: exact copy targets — destructure, terminal-save attempts, recursion, delete helpers, 400 photo check placement)
    - .planning/phases/03-working-images-step/03-RESEARCH.md (lines 212-241 condensed recursive pattern; lines 371-373 Pitfall 4 — 400 vs 500)
  </read_first>
  <action>
    In the handler msg destructure (currently line 25), add `attemptsLog = []`: `const { article, size, imageType, attempt = 1, feedback = [], force = false, attemptsLog = [] } = msg;`.

    Add a missing-photo guard in the handler body BEFORE the generateImage call (after sizeRecord is resolved, around current line 36-49): `const photoNames = await store.listArtifacts(article, 'photos', 1); if (!photoNames || photoNames.length === 0) return respond(400, { error: 'no mold photo found' });` (D-03 — returns 400 not 500, per RESEARCH OQ-1/Pitfall 4). Note the stub path inside generateImage does not need photos, but for the 400 contract we check at the handler level unconditionally so a missing photo always yields 400 regardless of API key.

    In the terminal save branch (current line 83), replace `attempts: buildAttemptsLog(stepMeta, attempt, criticVerdict)` with `attempts: [...attemptsLog, { attempt, criticVerdict }]` (D-11).

    Replace the critic-reject branch (current lines 99-101: `await enqueueRetry({...}); return respond(202, {queued:true...});`) with the direct recursion (D-10): `return exports.handler({ body: JSON.stringify({ article, size, imageType, attempt: attempt + 1, feedback: criticVerdict.issues, force, attemptsLog: [...attemptsLog, { attempt, criticVerdict }] }) });`.

    DELETE the enqueueRetry helper (current lines 194-209) — this removes the only @aws-sdk/client-sqs usage in the file. DELETE the buildAttemptsLog helper (current lines 227-230).

    KEEP unchanged: parseMessage, respond, sha256, runCritic (Claude Vision stub returning ok:true without ANTHROPIC_API_KEY — D-04), the cache check (lines 42-47), putArtifact/updateManifest save calls.
  </action>
  <verify>
    <automated>node --test 'test/step-images.test.js'</automated>
  </verify>
  <acceptance_criteria>
    - `node --test 'test/step-images.test.js'` exits ZERO (all RED tests from Plan 03-01 now GREEN)
    - `grep -v '^#' functions/step-images/index.js | grep -c 'enqueueRetry'` returns 0
    - `grep -v '^#' functions/step-images/index.js | grep -c 'buildAttemptsLog'` returns 0
    - `grep -c '@aws-sdk/client-sqs' functions/step-images/index.js` returns 0
    - The handler contains `exports.handler({ body: JSON.stringify(` in the critic-reject branch (recursive self-call)
    - The handler contains `attemptsLog = []` in the msg destructure and `[...attemptsLog, { attempt, criticVerdict }]` in the terminal save
    - The handler returns `respond(400, { error: 'no mold photo found' })` when listArtifacts('photos',1) is empty
  </acceptance_criteria>
  <done>Local recursive retry works, attempts[] threads correctly, missing photo yields 400, and YMQ/legacy helpers are gone.</done>
</task>

<task type="auto">
  <name>Task 3: prompts.images.json composition instruction + IMAGE_TYPES single-type</name>
  <files>layers/shared/config/prompts.images.json, functions/api/index.js</files>
  <read_first>
    - layers/shared/config/prompts.images.json (current — {{faceSize}} appears in main/infographic/scale prompts lines 4-6; feedbackSuffix line 9)
    - functions/api/index.js (line 12 IMAGE_TYPES constant; lines 369-392 dispatch loop using it — UNCHANGED)
    - .planning/phases/03-working-images-step/03-PATTERNS.md (lines 81-106: prompts.images.json changes + IMAGE_TYPES single change)
    - layers/shared/config/prompts.texts.json (sibling convention for {{topic}}/{{purpose}} token names)
  </read_first>
  <action>
    In layers/shared/config/prompts.images.json:
    - Replace EVERY occurrence of {{faceSize}} with {{moldSize}} across the main, infographic, and scale prompts (D-09 — keep all 4 prompt entries per D-06).
    - Rewrite the `infographic` prompt value from a text-to-image description into an image-in-image COMPOSITION INSTRUCTION (Claude's Discretion): it must instruct the model to place the supplied mold photo(s) onto the provided background template, specify where the mold photo goes and where text blocks (mold name «{{moldName}}», dimensions {{moldLength}}x{{moldWidth}}x{{moldHeight}} cm, mold size {{moldSize}}) are positioned, and reflect the mold type/use via {{topic}} and {{purpose}} (D-12). The prompt MUST contain the literal tokens {{moldName}}, {{moldSize}}, {{moldLength}}, {{moldWidth}}, {{moldHeight}}, {{topic}}, {{purpose}} so the generateImage substitution chain resolves them. Do NOT include {{faceSize}}.
    - KEEP the feedbackSuffix key name and its {{issues}} token unchanged.

    In functions/api/index.js line 12: change `const IMAGE_TYPES = ['main', 'infographic', 'scale', 'lifestyle'];` to `const IMAGE_TYPES = ['infographic'];` (D-05). Leave the dispatch loop (lines 369-392) untouched — with one type it yields exactly 5 messages.
  </action>
  <verify>
    <automated>node -e "const p=require('./layers/shared/config/prompts.images.json'); const ig=p.prompts.infographic; const ok=['{{moldName}}','{{moldSize}}','{{topic}}','{{purpose}}','{{moldLength}}'].every(t=>ig.includes(t)) && !JSON.stringify(p).includes('{{faceSize}}'); const api=require('fs').readFileSync('functions/api/index.js','utf8'); process.exit(ok && /const IMAGE_TYPES = \['infographic'\]/.test(api)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - prompts.images.json contains NO `{{faceSize}}` anywhere (D-09)
    - The infographic prompt contains the tokens {{moldName}}, {{moldSize}}, {{moldLength}}, {{moldWidth}}, {{moldHeight}}, {{topic}}, {{purpose}}
    - The infographic prompt reads as a composition instruction referencing the background and the mold photo (not a standalone text-to-image description)
    - feedbackSuffix key and its {{issues}} token are preserved
    - functions/api/index.js line 12 is exactly `const IMAGE_TYPES = ['infographic'];`
    - prompts.images.json remains valid JSON (require() succeeds)
  </acceptance_criteria>
  <done>Infographic prompt is a composition instruction with correct tokens, no faceSize remains, and the dispatch produces exactly 5 messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler → filesystem (`templates/${imageType}.png`) | `imageType` interpolated into a file path |
| handler → OpenAI Edits API | outbound request with API key + image buffers |
| handler → versionStore (photos) | reads stored mold-photo blobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `fs.readFileSync(path.join(SHARED,'templates',`${imageType}.png`))` | mitigate | `imageType` originates from the server-side IMAGE_TYPES allowlist via dispatch (functions/api/index.js), never raw user input; no path-traversal vector. Do not accept arbitrary imageType from the request without allowlist membership. |
| T-03-02 | Tampering | photo filename in `getArtifact('photos',1,name)` | accept | Filenames are sanitized at upload (api/index.js `replace(/[^a-zA-Z0-9._-]/g,'_')`) and `listArtifacts` returns only stored names; no new vector introduced. |
| T-03-03 | Information disclosure | OpenAI request with Authorization header | mitigate | Never `console.*` the request headers or API key; existing logs emit only generation metadata (article/size/imageType). |
| T-03-04 | Spoofing/SSRF | OpenAI endpoint URL | accept | URL is a hardcoded constant `https://api.openai.com/v1/images/edits`; no user-controlled host. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (Node built-ins only); slopcheck N/A — RESEARCH Package Legitimacy Audit marked N/A. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-02)

Modified files:
- `functions/step-images/index.js` — rewritten compositor
- `layers/shared/config/prompts.images.json` — composition-instruction infographic prompt, {{faceSize}}→{{moldSize}}
- `functions/api/index.js` — `IMAGE_TYPES = ['infographic']`

New exported symbols:
- `exports.buildEditRequest(article, sizeRecord, imageType, feedback)` → `Promise<{ prompt, imageCount }>` in functions/step-images/index.js

Changed function signatures:
- `generateImage(article, sizeRecord, imageType, feedback)` — added leading `article` parameter

Deleted symbols:
- `enqueueRetry` (removed YMQ/SQS path)
- `buildAttemptsLog` (replaced by inline `[...attemptsLog, ...]`)
</artifacts_produced>

<verification>
- `node --test 'test/step-images.test.js'` exits zero
- `grep -c '@aws-sdk/client-sqs' functions/step-images/index.js` returns 0
- prompts.images.json and functions/api/index.js both require()/parse without error
- No new npm packages (package.json dependencies unchanged)
</verification>

<success_criteria>
- IMG-02: step-images produces a real infographic slide per size composited from background + photo (via Edits API or stub in test mode)
- IMG-03: mold photo buffers are included in the Edits API image[] (buildEditRequest.imageCount >= 2)
- All four carry-forward bugs fixed; D-05/D-06/D-09/D-12 honored
</success_criteria>

<output>
Create `.planning/phases/03-working-images-step/03-02-SUMMARY.md` when done
</output>
