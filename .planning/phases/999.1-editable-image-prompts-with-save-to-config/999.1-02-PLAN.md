---
phase: 999.1-editable-image-prompts-with-save-to-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/step-images/index.js
  - test/step-images.test.js
autonomous: true
requirements: [D-02, D-04, D-05, D-12]
must_haves:
  truths:
    - "step-images sources the generation prompt from manifest.steps['03-images'].slidesConfig.slides[slideId].generatedPrompt, not from require('prompts.images.json')"
    - "When no slidesConfig exists, the default image types (main/infographic/scale/lifestyle) still generate using the on-disk defaults (backward compatible)"
    - "A custom slide (id custom-*) generates without reading a template file and names its artifact {size}_{slideId}.png"
    - "An edited generatedPrompt invalidates the cache so regeneration does not silently skip"
  artifacts:
    - path: "functions/step-images/index.js"
      provides: "slide-driven prompt source, per-size dimension append, custom-slide path, {size}_{slideId} naming, slideId in message shape"
      exports: ["handler", "buildEditRequest"]
    - path: "test/step-images.test.js"
      provides: "Extended assertions: prompt sourced from slidesConfig; custom-slide naming"
      contains: "slidesConfig"
  key_links:
    - from: "functions/step-images/index.js"
      to: "layers/shared/versionStore.js"
      via: "store.getManifest(article) → steps['03-images'].slidesConfig.slides.find(s => s.id === slideKey)"
      pattern: "slidesConfig"
---

<objective>
Make `functions/step-images/index.js` slide-aware: the per-invocation generation prompt comes from the per-article `slidesConfig` stored in the manifest (written by Plan 01), falling back to the on-disk defaults only when no config exists. Add per-size dimension injection so a once-authored per-article prompt still carries correct physical data per size, handle custom slides (no template file, `{size}_{slideId}.png` naming), and fix the cache key so edited prompts regenerate.

Purpose: This is the load-bearing architectural shift of the phase (D-02) — prompts become editable per line. Everything else is additive UI/routing.
Output: modified `functions/step-images/index.js` + extended `test/step-images.test.js`.
</objective>

<execution_context>
@D:/Molds/mp-card-creator/.claude/gsd-core/workflows/execute-plan.md
@D:/Molds/mp-card-creator/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-CONTEXT.md
@.planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-RESEARCH.md
@.planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02)

- Message shape gains optional `slideId`: `{ article, size, slideId?, imageType?, attempt, feedback?, force?, runVersion? }`. `slideKey = slideId || imageType`.
- `buildEditRequest(article, sizeRecord, slideKey, feedback)` resolves the prompt from `slidesConfig` (fallback to on-disk defaults) and appends a resolved per-size dimension block.
- Artifact naming generalised from `{size}_{imageType}.png` to `{size}_{slideKey}.png`.
- Custom-slide generation path that skips the `SHARED/templates/{imageType}.png` read.
- `inputHash` includes the slide's `generatedPrompt` so edits bust the cache.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend step-images.test.js for slidesConfig-sourced prompt + custom-slide naming</name>
  <files>test/step-images.test.js</files>
  <read_first>
    - test/step-images.test.js (existing harness, `createLine`, `readSizeRecord`, `runImages`, `readManifest`; note OPENAI_API_KEY is deleted so real generation is not testable — assert on buildEditRequest, not pixels)
    - functions/step-images/index.js (current buildEditRequest signature `(article, sizeRecord, imageType, feedback)`; substitutePrompt token substitution; artifact naming line 81)
    - .planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-RESEARCH.md (Pitfall 3 per-size append, Pitfall 5 custom slide, Pitfall 6 cache key)
  </read_first>
  <behavior>
    - New test D-02-source: seed a slidesConfig into the manifest (via `store.updateManifest(article,'03-images',{ slidesConfig:{ feedbackSuffix:'', slides:[{ id:'infographic', label:'x', description:'d', generatedPrompt:'DISTINCTMARKER prompt text', files:[], default:true }] } })`), then call `buildEditRequest(article, sizeRecord, 'infographic', [])` and assert the returned prompt STARTS FROM / contains `DISTINCTMARKER prompt text` (proves the prompt came from slidesConfig, not the on-disk template) AND contains the size's numeric dimensions (proves per-size append).
    - New test D-02-fallback: on a line with NO slidesConfig, `buildEditRequest(article, sizeRecord, 'infographic', [])` still returns a non-empty prompt with no unresolved `{{...}}` (backward-compatible default path).
    - New test custom-slide: seed a slidesConfig containing a slide `{ id:'custom-abc', generatedPrompt:'CUSTOMMARKER', ... }`, call `buildEditRequest(article, sizeRecord, 'custom-abc', [])` and assert it returns the custom prompt without throwing (no template lookup on the buildEditRequest path).
  </behavior>
  <action>
    Append three `test(...)` cases to `test/step-images.test.js` implementing the behaviors above. Import `store` the same way the file already resolves shared modules (via SHARED_LAYER_PATH) or require `../layers/shared/versionStore` to seed slidesConfig directly (no API call needed — this isolates step-images). Use unique article ids per test. Assert with the distinctive marker strings so a pass unambiguously proves the prompt was sourced from slidesConfig. Do NOT assert on generated pixels or full-handler generation (no key offline). These new cases MUST fail before Task 2 (current buildEditRequest ignores slidesConfig) — RED. Leave the existing passing cases untouched.
  </action>
  <verify>
    <automated>node --test test/step-images.test.js 2>&1 | grep -Eq '(not ok|fail)' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/step-images.test.js` contains three new cases referencing `slidesConfig` and the marker strings.
    - The three new cases fail before Task 2 (RED); the existing cases' pass/fail status is unchanged from the documented baseline.
    - No new case requires OPENAI_API_KEY or network.
  </acceptance_criteria>
  <done>Three new RED tests assert prompt-from-slidesConfig, default fallback, and custom-slide handling via buildEditRequest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slide-driven prompt source, per-size append, custom slides, {size}_{slideId} naming, cache-key fix</name>
  <files>functions/step-images/index.js</files>
  <read_first>
    - functions/step-images/index.js (WHOLE FILE — parseMessage line 364; message destructure line 29; imageType-required guard line 30; inputHash line 46; cache check lines 49-54; substitutePrompt lines 126-139; buildEditRequest lines 148-159; generateImage lines 171-187; generateImageOpenAI template read line 285; artifact naming line 81; success updateManifest lines 96-102)
    - layers/shared/versionStore.js (getManifest; updateManifest)
    - .planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-RESEARCH.md (Pattern 2, Pitfall 3/5/6; Assumption A1 recommendation to append per-size data)
    - .planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-PATTERNS.md (step-images slide-driven section)
  </read_first>
  <behavior>
    - After this task the three new step-images.test.js cases pass, and the existing buildEditRequest-based cases (D-09/IMG-03) still pass.
  </behavior>
  <action>
    Accept `slideId` in the message: extend the destructure at line 29 to include `slideId`, and compute `const slideKey = slideId || imageType`. Relax the guard so a request is valid when either slideId or imageType is present (return 400 only when both are missing).

    Add a helper `async function resolveSlidePrompt(article, slideKey, sizeRecord)` that reads `manifest.steps['03-images'].slidesConfig` via `store.getManifest(article)`, finds the slide whose `id === slideKey`, and returns its `generatedPrompt` when present and non-empty. When no slidesConfig or no matching slide is found, fall back to the current on-disk default behavior (`substitutePrompt(sizeRecord, slideKey)`), preserving backward compatibility for the four default image types (D-02/D-03). This keeps step-images decoupled from Plan 01's buildDefaultSlides.

    Change `buildEditRequest(article, sizeRecord, slideKey, feedback=[])` to source its base prompt from `resolveSlidePrompt(...)` instead of calling `substitutePrompt` directly. After obtaining the base prompt, append a resolved per-size dimension block built from the sizeRecord (dimensions L×W×H in cm and mold size) so a once-authored per-article prompt still carries correct physical data per size (Pitfall 3 / A1). Keep the existing feedbackSuffix append and the `imageCount = 1 + photoCount` return. Keep buildEditRequest exported.

    Update `generateImage(article, sizeRecord, slideKey, feedback)` to build its prompt through the same resolve+append path (call buildEditRequest or a shared prompt builder rather than the standalone substitutePrompt) so the actual generation uses the edited prompt. For custom slides (slideKey starting with `custom-`), route generation away from the template-reading OpenAI edits fallback: prefer the apiframe / images/generations text+photo path and do not read `SHARED/templates/{slideKey}.png` (which does not exist for custom ids) — pass the slideKey through so `generateImageOpenAI` only attempts a template read for the four default ids.

    Generalise artifact naming at the save site (line 81) from `${size}_${imageType}.png` to `${size}_${slideKey}.png`, and record `slideId: slideKey` (in addition to/renaming from `imageType`) on the history entry so downstream artifact lookup and the frontend grid can key on slideKey. Keep writing `imageType: slideKey` as well if needed for backward-compatible history matching in handleGetArtifact.

    Fix the cache key (Pitfall 6): include the resolved slide `generatedPrompt` (or the full resolved prompt) in the `inputHash` computation at line 46 so editing a prompt invalidates the cache and regeneration is not silently skipped.

    Preserve unchanged: parseMessage, the recursive local-retry, pushHistory, the success-path `error:null/failedAt:null` clear, and the top-level try/catch that records `{error, failedAt}`.
  </action>
  <verify>
    <automated>node --test test/step-images.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The three new slidesConfig cases pass; the pre-existing buildEditRequest cases (no unresolved `{{...}}`, imageCount>=2) still pass.
    - Source assertion: `functions/step-images/index.js` contains a `slidesConfig` read (grep `slidesConfig` matches) and an artifact-name expression using `slideKey` (grep for `${size}_${slideKey}`-style naming).
    - buildEditRequest returns the slidesConfig-authored prompt (marker preserved) with the per-size dimensions appended; on a line without slidesConfig it returns the default prompt with no `{{...}}`.
    - `npm test` shows no NEW failures beyond the documented 4-test offline step-images baseline; the new slidesConfig cases are green.
  </acceptance_criteria>
  <done>step-images sources prompts from slidesConfig with default fallback, appends per-size dimensions, handles custom slides without a template read, names artifacts {size}_{slideId}.png, and busts cache on prompt edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest (slidesConfig) → step-images | Stored `generatedPrompt` (originally user-authored) is sent to the image API |
| step-images → templates dir | `slideKey` was previously used as a filename segment for template reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-05 | Tampering | template path from slideKey | mitigate | Only the four fixed default ids ever reach the `SHARED/templates/{id}.png` read; custom-* ids skip the template path entirely, so slideKey never forms an arbitrary template path |
| T-999.1-06 | DoS | recursive retry loop | accept | Existing MAX_ATTEMPTS=3 bound unchanged; no new unbounded recursion introduced |
| T-999.1-07 | Info-disclosure | generatedPrompt content to image API | accept | Prompt is user-authored and user-reviewed; same trust level as the existing on-disk prompts |
</threat_model>

<verification>
- `node --test test/step-images.test.js` — new slidesConfig cases green; existing buildEditRequest cases green.
- `npm test` — no increase over the documented offline baseline (4 pre-existing step-images generation failures).
- Grep confirms `slidesConfig` read and `slideKey`-based artifact naming in the source.
</verification>

<success_criteria>
- Generation prompt is sourced from the per-article slidesConfig (D-02), with a safe on-disk default fallback.
- Per-size dimensions are appended so per-article prompts stay physically accurate across XS–XL.
- Custom slides generate and are named {size}_{slideId}.png without a template file.
- Edited prompts invalidate the cache.
</success_criteria>

<output>
Create `.planning/phases/999.1-editable-image-prompts-with-save-to-config/999.1-02-SUMMARY.md` when done.
</output>
