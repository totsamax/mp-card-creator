---
phase: 999.1
slug: editable-image-prompts-with-save-to-config
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-03
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `node:test` + `node:assert` |
| **Config file** | none — glob in npm script (`"test": "node --test 'test/**/*.test.js'"`) |
| **Quick run command** | `node --test test/slides.test.js` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `node --test test/slides.test.js`
- **After every plan wave:** Run `npm test`
- **Before `/gsd-verify-work`:** Full suite must be green (4 pre-existing `step-images` offline failures are known baseline — do not attribute to this phase)
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| D-03/D-10 | TBD | 1 | — | — | GET /slides returns defaults when unset | integration | `node --test test/slides.test.js` | ❌ Wave 0 | ⬜ pending |
| D-10 | TBD | 1 | — | — | POST /slides persists + GET round-trips | integration | `node --test test/slides.test.js` | ❌ Wave 0 | ⬜ pending |
| D-11 | TBD | 1 | — | — | generate-prompt returns a string (no-key stub) | unit | `node --test test/slides.test.js` | ❌ Wave 0 | ⬜ pending |
| D-12 | TBD | 1 | — | — | slide regenerate returns 202 (route contract) | integration | `node --test test/slides.test.js` | ❌ Wave 0 | ⬜ pending |
| D-02 | TBD | 1 | — | — | step-images reads prompt from slidesConfig, not require() | integration | `node --test test/step-images.test.js` (extend) | ⚠ extend existing | ⬜ pending |
| D-09 | TBD | 1 | — | — | file upload stores blob + returns ref, retrievable | integration | `node --test test/slides.test.js` | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/slides.test.js` — covers D-03, D-10, D-11 (no-key stub), D-12, D-09; model after `test/step-images.test.js` (STORE_ADAPTER=local, per-article unique names, delete OPENAI_API_KEY)
- [ ] Extend `test/step-images.test.js` — assert generation prompt is sourced from `slidesConfig`, and custom-slide naming `{size}_{slideId}.png`
- [ ] Fixtures: reuse `test/fixtures/test-mold.png` for slide-file upload tests
- [ ] Frontend (`frontend/`): no test runner configured — UI changes verified via `/gsd-verify-work` UAT, not automated tests

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Prompt editor renders and is editable in UI | D-01 | No frontend test runner | Open PipelineApp, navigate to step-images panel, verify textarea is editable per each slide |
| Slide reorder / add / delete in UI | D-05/D-06/D-07 | No frontend test runner | Add a slide, reorder, delete, verify changes persist after save |
| Custom slide image upload flow | D-08/D-09 | File upload UX | Upload a PNG via UI, verify reference appears in slide card |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
