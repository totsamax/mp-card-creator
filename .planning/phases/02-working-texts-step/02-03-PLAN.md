---
phase: 02-working-texts-step
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - functions/step-texts/index.js
  - layers/shared/config/prompts.texts.json
  - layers/shared/config/prompts.critic-texts.json
autonomous: true
requirements: [TXT-01, TXT-02]
must_haves:
  truths:
    - "The prompt substitution chain replaces {{moldSize}}, {{topic}}, {{purpose}} and no longer references {{faceSize}}"
    - "Generated texts for moldType=hands carry the hands topic/purpose context and contain no unresolved {{...}} placeholders"
    - "All 5 sizes (XS-XL) each produce a {size}_texts.json artifact"
    - "runCritic accepts a topic argument and applies topicKeywordCheck + noUnresolvedPlaceholders; runCritic is exported for unit testing"
  artifacts:
    - path: "functions/step-texts/index.js"
      provides: "Fixed substitution chain (BUG-01), runCritic(texts, topic) with DEC-03 rules, exported runCritic"
      contains: "runCritic"
      min_lines: 200
    - path: "layers/shared/config/prompts.texts.json"
      provides: "{{topic}} and {{purpose}} placeholders in user prompt"
      contains: "{{topic}}"
    - path: "layers/shared/config/prompts.critic-texts.json"
      provides: "topicKeywordCheck and noUnresolvedPlaceholders rule config"
      contains: "topicKeywordCheck"
  key_links:
    - from: "functions/step-texts/index.js (generateTexts)"
      to: "sizeRecord.topic / sizeRecord.purpose / sizeRecord.moldSize"
      via: ".replace() substitution chain"
      pattern: "replace\\('\\{\\{(topic|purpose|moldSize)\\}\\}'"
    - from: "functions/step-texts/index.js (handler)"
      to: "runCritic"
      via: "runCritic(generated, sizeRecord.topic)"
      pattern: "runCritic\\(generated, sizeRecord\\.topic\\)"
    - from: "functions/step-texts/index.js (runCritic)"
      to: "layers/shared/config/prompts.critic-texts.json"
      via: "criticRules.topicKeywordCheck / criticRules.noUnresolvedPlaceholders"
      pattern: "criticRules\\.(topicKeywordCheck|noUnresolvedPlaceholders)"
---

<objective>
Implement DEC-02 + DEC-03 + BUG-01: make the prompt moldType-aware and the critic smarter, so texts for all 5 sizes are generated with the correct mold context and no leftover placeholders.

Three coordinated changes: (1) fix the substitution chain — drop `{{faceSize}}` (undefined since Phase 1), add `{{moldSize}}`, `{{topic}}`, `{{purpose}}`; (2) add `{{topic}}`/`{{purpose}}` lines to `prompts.texts.json`; (3) extend `runCritic(texts, topic)` with the two DEC-03 rules and export it. This turns the substitution/5-size/critic tests from Plan 01 GREEN.

Purpose: TXT-01 (texts for all 5 sizes from master data) and TXT-02 (prompts respect moldType — no "лицо" for hands).
Output: Modified handler + two config files. Phase suite fully GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-texts-step/02-CONTEXT.md
@.planning/phases/02-working-texts-step/02-RESEARCH.md
@.planning/phases/02-working-texts-step/02-PATTERNS.md
@functions/step-texts/index.js
@layers/shared/config/prompts.texts.json
@layers/shared/config/prompts.critic-texts.json
@layers/shared/templateEngine.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix substitution chain (BUG-01) and add topic/purpose to prompts.texts.json (DEC-02)</name>
  <files>functions/step-texts/index.js, layers/shared/config/prompts.texts.json</files>
  <read_first>
    - functions/step-texts/index.js (generateTexts substitution chain lines 106-115 — the `{{faceSize}}` bug at line 109)
    - layers/shared/config/prompts.texts.json (current user prompt — note {{moldSize}} is ALREADY present at the "Характерный размер молда" line; the bug is in JS, not JSON)
    - layers/shared/templateEngine.js (lines 117-135 — confirms sizeRecord has moldSize, topic, purpose and has NO faceSize field)
    - .planning/phases/02-working-texts-step/02-CONTEXT.md (DEC-02, BUG-01)
    - .planning/phases/02-working-texts-step/02-PATTERNS.md (substitution-chain before/after; "do NOT add a second {{moldSize}} line to JSON")
  </read_first>
  <behavior>
    - generateTexts substitutes {{moldSize}} with sizeRecord.moldSize (was {{faceSize}}/undefined).
    - generateTexts substitutes {{topic}} with sizeRecord.topic and {{purpose}} with sizeRecord.purpose.
    - The rendered userPrompt contains no unresolved {{...}} tokens for a valid sizeRecord.
    - For moldType=hands, the prompt carries the hands topic/purpose context (so output avoids "лицо").
  </behavior>
  <action>
    In `generateTexts` (functions/step-texts/index.js), edit the `.replace()` chain: REMOVE the line `.replace('{{faceSize}}', sizeRecord.faceSize)` (faceSize does not exist on sizeRecord — it renders undefined). ADD `.replace('{{moldSize}}', sizeRecord.moldSize)` in its place, and ADD `.replace('{{topic}}', sizeRecord.topic)` and `.replace('{{purpose}}', sizeRecord.purpose)` before the existing `.replace('{{feedbackBlock}}', feedbackBlock)` line. Keep all other existing replaces (moldName, theme, moldLength, moldWidth, moldHeight, color, brand) unchanged.

    In `layers/shared/config/prompts.texts.json`, in the `generate.user` string, append two lines to the "Данные товара" block (after the "Бренд: {{brand}}" segment): "- Тема товара: {{topic}}" and "- Назначение: {{purpose}}" (as part of the same `\n`-joined user string). Do NOT add a second {{moldSize}} line — {{moldSize}} is already present in the template (only the JS code had the faceSize bug). Keep the JSON valid (single user string, proper escaping).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('layers/shared/config/prompts.texts.json','utf8'))" && grep -q '{{topic}}' layers/shared/config/prompts.texts.json && grep -q '{{purpose}}' layers/shared/config/prompts.texts.json && grep -v '^#' functions/step-texts/index.js | grep -q 'faceSize' && echo "FAIL faceSize present" || echo "OK no faceSize"</automated>
  </verify>
  <acceptance_criteria>
    - `functions/step-texts/index.js` does NOT contain `faceSize`.
    - `functions/step-texts/index.js` contains `.replace('{{moldSize}}', sizeRecord.moldSize)`, `.replace('{{topic}}', sizeRecord.topic)`, and `.replace('{{purpose}}', sizeRecord.purpose)`.
    - `layers/shared/config/prompts.texts.json` parses as valid JSON and contains both `{{topic}}` and `{{purpose}}`.
    - `prompts.texts.json` contains exactly one `{{moldSize}}` occurrence (no duplicate line added).
    - Plan 01 test "TXT-02: no unresolved {{...}} placeholders" passes.
  </acceptance_criteria>
  <done>The substitution chain renders moldSize/topic/purpose, faceSize is gone, prompts.texts.json carries topic+purpose context, and the no-placeholder test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend and export runCritic with DEC-03 rules; pass topic at call site; add critic config</name>
  <files>functions/step-texts/index.js, layers/shared/config/prompts.critic-texts.json</files>
  <read_first>
    - functions/step-texts/index.js (runCritic lines 191-215; call site line 58 `runCritic(generated)`; module.exports/exports at top — confirm only `exports.handler` is currently exported)
    - layers/shared/config/prompts.critic-texts.json (current rules/requiredSubstrings/bannedPhrases shape)
    - .planning/phases/02-working-texts-step/02-CONTEXT.md (DEC-03 config shape — topicKeywordCheck, noUnresolvedPlaceholders)
    - .planning/phases/02-working-texts-step/02-RESEARCH.md (Pattern 3 — topicWords split `[\s,]+`, length >= 4 threshold is Claude's Discretion; existing length/requiredSubstrings/bannedPhrases checks stay unchanged)
    - .planning/phases/02-working-texts-step/02-PATTERNS.md (runCritic before/after; backward-compatible optional topic param)
  </read_first>
  <behavior>
    - runCritic(texts, topic): existing length/requiredSubstrings/bannedPhrases checks unchanged.
    - topicKeywordCheck (when enabled and topic present): titleFull must contain at least one topic word (lowercased, split on whitespace/comma, words length >= 4); otherwise push the configured message.
    - noUnresolvedPlaceholders (when enabled): annotation must not match the configured `{{...}}` pattern; otherwise push the message.
    - runCritic returns { ok: issues.length === 0, issues }.
    - runCritic is exported so the Plan 01 unit tests can import it.
    - Handler calls runCritic(generated, sizeRecord.topic).
  </behavior>
  <action>
    In `runCritic` (functions/step-texts/index.js), change the signature to `function runCritic(texts, topic)`. Keep the three existing loops (length rules, requiredSubstrings, bannedPhrases) exactly as-is. After them, add DEC-03 rule 1 — topicKeywordCheck: read `const tk = criticRules.topicKeywordCheck;` and if `tk?.enabled && topic`, compute `topicWords = topic.toLowerCase().split(/[\s,]+/).filter(w => w.length >= 4)`, read `fieldVal = (texts[tk.field] ?? '').toLowerCase()`, and if no topicWord is included in fieldVal, push `tk.message`. Add DEC-03 rule 2 — noUnresolvedPlaceholders: read `const np = criticRules.noUnresolvedPlaceholders;` and if `np?.enabled`, build `new RegExp(np.pattern)` and if it tests true against `texts[np.field] ?? ''`, push `np.message`. Return the same `{ ok, issues }` object.

    Update the call site (line 58) from `runCritic(generated)` to `runCritic(generated, sizeRecord.topic)`.

    Export runCritic for unit testing: add `runCritic` to the module's exports (e.g., set `exports.runCritic = runCritic;`) without disturbing `exports.handler`.

    In `layers/shared/config/prompts.critic-texts.json`, add two top-level keys after `bannedPhrases`: `topicKeywordCheck` = { "enabled": true, "field": "titleFull", "message": "titleFull не содержит ключевых слов из темы товара" } and `noUnresolvedPlaceholders` = { "enabled": true, "field": "annotation", "pattern": "\\{\\{[^}]+\\}\\}", "message": "annotation содержит нераскрытый плейсхолдер" }. Keep existing rules/requiredSubstrings/bannedPhrases unchanged. Ensure valid JSON.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('layers/shared/config/prompts.critic-texts.json','utf8'))" && node -e "const m=require('./functions/step-texts/index.js'); if(typeof m.runCritic!=='function')process.exit(1); const r=m.runCritic({titleShort:'x',titleFull:'силиконовый молд',annotation:'нераскрыт {{x}}'},'Руки куклы, кисти рук'); process.exit(r.ok===false?0:1)" && node --test 'test/step-texts.test.js'</automated>
  </verify>
  <acceptance_criteria>
    - `layers/shared/config/prompts.critic-texts.json` parses as valid JSON and contains `topicKeywordCheck` and `noUnresolvedPlaceholders`.
    - `functions/step-texts/index.js` defines `function runCritic(texts, topic)` and exports it (`exports.runCritic`).
    - The call site reads `runCritic(generated, sizeRecord.topic)`.
    - The file contains `criticRules.topicKeywordCheck` and `criticRules.noUnresolvedPlaceholders` references.
    - All Plan 01 tests pass: `node --test 'test/step-texts.test.js'` exits 0.
    - `npm test` (full suite incl. A/B/C regression) exits 0.
  </acceptance_criteria>
  <done>runCritic applies the two DEC-03 rules with topic context and is exported; the full Phase 2 test suite is GREEN and existing regression tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| questionnaire fields → prompt | moldName/theme/topic/purpose flow into the LLM prompt; low-risk single-team tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | unresolved placeholder leaking to LLM/output | mitigate | BUG-01 fix removes the undefined `{{faceSize}}` substitution; noUnresolvedPlaceholders critic rule catches any remaining `{{...}}` in annotation. Verified by Plan 01 no-placeholder test. |
| T-02-04 | Tampering | prompt injection via questionnaire moldName/theme | accept | LOW risk per RESEARCH Security Domain — internal single-user tool. No new sanitizers in scope. LLM response is JSON-parsed with try/catch + stub fallback (existing). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; only JS edits and JSON config additions. Zero new packages (RESEARCH audit). |
</threat_model>

<verification>
- `node --test 'test/step-texts.test.js'` exits 0 (all Phase 2 behaviors GREEN: substitution, 5 sizes, recursion, attempts, runCritic rules).
- `npm test` exits 0 (A/B/C regression GREEN).
- `grep -v '^#' functions/step-texts/index.js | grep -c faceSize` returns 0.
- Both config JSON files parse without error.
</verification>

<success_criteria>
- TXT-01: all 5 sizes (XS-XL) generate texts from master data.
- TXT-02: prompt carries moldType context (topic/purpose); moldType=hands output avoids "лицо"; no unresolved placeholders.
- DEC-03 critic rules active and unit-tested via exported runCritic.
- Full Phase 2 suite GREEN; existing regression tests unaffected.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol / File | Kind | Created/Modified | Notes |
|---------------|------|------------------|-------|
| `generateTexts` substitution chain (functions/step-texts/index.js) | function | modified | faceSize removed; moldSize/topic/purpose added |
| `runCritic` (functions/step-texts/index.js) | function | modified + exported | signature `(texts, topic)`; DEC-03 topicKeywordCheck + noUnresolvedPlaceholders; now `exports.runCritic` |
| handler runCritic call site (functions/step-texts/index.js) | call | modified | now passes `sizeRecord.topic` |
| `layers/shared/config/prompts.texts.json` | config | modified | `{{topic}}` + `{{purpose}}` lines added to user prompt |
| `layers/shared/config/prompts.critic-texts.json` | config | modified | `topicKeywordCheck` + `noUnresolvedPlaceholders` keys added |
</artifacts_produced>

<output>
Create `.planning/phases/02-working-texts-step/02-03-SUMMARY.md` when done.
</output>
