---
phase: 02-working-texts-step
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/step-texts.test.js
autonomous: true
requirements: [TXT-01, TXT-02, TXT-03]
must_haves:
  truths:
    - "Running `node --test 'test/step-texts.test.js'` discovers and executes the new test file"
    - "The test file fails (RED) against the current step-texts handler — proving it asserts new behavior not yet built"
    - "Tests cover all three requirements: 5-size generation (TXT-01), no unresolved placeholders / moldType context (TXT-02), recursion termination + attempts log (TXT-03)"
  artifacts:
    - path: "test/step-texts.test.js"
      provides: "RED test contract for phase 02 — recursion, substitution, attempts accumulation, 5 sizes, runCritic rules"
      contains: "step-texts"
      min_lines: 80
  key_links:
    - from: "test/step-texts.test.js"
      to: "functions/step-texts/index.js"
      via: "require('../functions/step-texts/index.js') and direct handler call"
      pattern: "require.*functions/step-texts"
    - from: "test/step-texts.test.js"
      to: "functions/api/index.js"
      via: "POST /lines to create master-data.json precondition"
      pattern: "require.*functions/api"
---

<objective>
Create the Wave 0 RED test contract for Phase 2 in `test/step-texts.test.js`. This is the phase's executable specification: it asserts every Phase 2 behavior (recursive local retry, faceSize→moldSize fix, topic/purpose injection, attempts[] accumulation, all-5-sizes generation, and the two new critic rules) BEFORE the implementation exists.

The file MUST fail (RED) when run against the current handler — that failure is the proof the test asserts new behavior. Plans 02 and 03 turn it GREEN.

Purpose: Per VALIDATION.md, no Phase 2 implementation task may be verified without this file. It closes the documented Wave 0 gap (`test/step-texts.test.js` does not exist).
Output: `test/step-texts.test.js` — a self-contained node:test file using `USE_STUB=true`, `STORE_ADAPTER=local`, unique article IDs per test, direct handler calls (never via api/runLocally).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-texts-step/02-CONTEXT.md
@.planning/phases/02-working-texts-step/02-RESEARCH.md
@.planning/phases/02-working-texts-step/02-PATTERNS.md
@.planning/phases/02-working-texts-step/02-VALIDATION.md
@test/create-line.smoke.test.js
@functions/step-texts/index.js
@functions/api/index.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test scaffold for step-texts handler (precondition + happy path + substitution)</name>
  <files>test/step-texts.test.js</files>
  <read_first>
    - test/create-line.smoke.test.js (exact env-setup header + multipart POST /lines pattern to create master-data.json precondition; reuse fixtures/test-mold.png)
    - functions/step-texts/index.js (handler signature, parseMessage event shape, USE_STUB stub path templateTexts, current `{{faceSize}}` bug at line 109)
    - functions/api/index.js (POST /lines handler used to seed master-data.json)
    - .planning/phases/02-working-texts-step/02-RESEARCH.md (Pitfalls 1,2,5 — version collision, fire-and-forget, cache-skip)
    - .planning/phases/02-working-texts-step/02-PATTERNS.md (test analog section, direct-handler-call pattern)
  </read_first>
  <action>
    Create `test/step-texts.test.js`. Copy the env-setup header verbatim from `test/create-line.smoke.test.js` lines 1-9 and ADD `process.env.USE_STUB = 'true';` so generation is deterministic (no network).

    Add a shared `createLine(article, moldType)` helper that calls `require('../functions/api/index.js').handler` with a multipart event identical in shape to create-line.smoke.test.js (httpMethod POST, path /lines, headers content-type multipart/form-data, files array with one entry reading `fixtures/test-mold.png`, formFields.questionnaire = JSON.stringify of a full questionnaire). Parameterize `article` and `moldType` so each test uses a UNIQUE article ID (avoids cache-skip Pitfall 5). The questionnaire MUST include the full 5-row `sizes` array (XS,S,M,L,XL) exactly as in the analog so step-normalize produces all 5 master-data records.

    Add a shared `runTexts(article, size, extra)` helper that calls `require('../functions/step-texts/index.js').handler({ body: JSON.stringify({ article, size, attempt: 1, force: true, ...extra }) })` and returns the parsed body. ALWAYS pass `force: true`. Call the handler DIRECTLY — never through api/runLocally (Pitfall 2: runLocally is fire-and-forget, not awaitable).

    Write these tests (they will FAIL against current code — that is required):
    - Test "TXT-02: generated texts contain no unresolved {{...}} placeholders" — createLine with moldType='hands', runTexts for size 'M', assert statusCode 200 and `!/\{\{[^}]+\}\}/.test(JSON.stringify(body.texts))`. (Fails today only if a placeholder leaks; with stub templateTexts it may pass — so ALSO assert that `body.texts.titleFull` is a non-empty string and does NOT equal the literal string containing `faceSize`.)
    - Test "TXT-02: moldType=hands texts do not mention 'лицо'/'личик'" — createLine moldType='hands', runTexts size 'M', assert lowercased JSON.stringify(body.texts) does not include 'лицо' and does not include 'личик'.
    - Test "TXT-01: all 5 sizes (XS-XL) produce a texts artifact" — createLine once, loop over ['XS','S','M','L','XL'] calling runTexts, assert each returns statusCode 200 with `body.texts` present. (Do NOT assert all files live in the same version folder — Pitfall 1 version collision.)

    Do NOT reference enqueueRetry or any implementation-internal symbol. Tests assert observable handler output and manifest state only.
  </action>
  <verify>
    <automated>node --test 'test/step-texts.test.js'; test $? -ne 0 &amp;&amp; echo RED-AS-EXPECTED || echo "WARN: expected failures"</automated>
  </verify>
  <acceptance_criteria>
    - `test/step-texts.test.js` exists and `node --test 'test/step-texts.test.js'` discovers it (output shows "tests" count > 0, not "tests 0").
    - The file contains `require('../functions/step-texts/index.js')` and `require('../functions/api/index.js')`.
    - The file sets `process.env.USE_STUB = 'true'` and `process.env.STORE_ADAPTER = 'local'`.
    - Every `runTexts` call passes `force: true`.
    - At least 3 test() blocks covering TXT-01 and TXT-02 are present.
    - No test asserts that all 5 size artifacts share one version folder.
  </acceptance_criteria>
  <done>Test file runs, discovers >0 tests, and the TXT-01/TXT-02 cases are present and runnable (currently failing or RED is acceptable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED tests for recursion termination, attempts[] accumulation, and runCritic rules</name>
  <files>test/step-texts.test.js</files>
  <read_first>
    - functions/step-texts/index.js (MAX_ATTEMPTS=3 const, runCritic signature, buildAttemptsLog reading stepMeta.history.slice(-1), updateManifest 3-arg call at line 81)
    - .planning/phases/02-working-texts-step/02-RESEARCH.md (Pitfall 3 updateManifest signature, Pitfall 4 attempts accumulation, Security Domain — recursion DoS guard `attempt < MAX_ATTEMPTS`)
    - .planning/phases/02-working-texts-step/02-CONTEXT.md (DEC-01 recursion, DEC-03 critic rules)
    - layers/shared/config/prompts.critic-texts.json (current rule shape; topicKeywordCheck/noUnresolvedPlaceholders not yet present)
  </read_first>
  <action>
    Extend `test/step-texts.test.js` with the recursion + critic-rule tests. These assert behavior that does not exist yet (recursion replacing enqueueRetry; topicKeywordCheck; noUnresolvedPlaceholders) and must FAIL today.

    Add a helper `readManifest(article)` that reads `path.join(process.env.OUTPUT_DIR, article, 'manifest.json')` via fs and JSON.parse — for inspecting `steps['02-texts'].history`.

    Tests to add:
    - Test "TXT-03: critic rejection recurses up to MAX_ATTEMPTS then stops with needsReview" — force a critic rejection deterministically. Use a moldType whose `topic` keywords will NOT appear in the stub `titleFull` so `topicKeywordCheck` fires every attempt (per DEC-03 the stub texts come from sizeRecord.titleFull which may not contain topic words). createLine, runTexts size 'M', assert final statusCode 200 (terminal save branch, not 202), and assert `body.needsReview === true`. This proves the recursion TERMINATES at attempt 3 (DoS guard `attempt < MAX_ATTEMPTS`).
    - Test "TXT-03: manifest history attempts[] records every attempt verdict" — after the forced-rejection run above, readManifest, take the last `steps['02-texts'].history` entry, assert its `attempts` array length === 3 (one verdict per attempt) and each element has an `attempt` number and a `criticVerdict` object.
    - Test "DEC-03 runCritic: noUnresolvedPlaceholders flags annotation containing {{x}}" — require the handler module and invoke its exported `runCritic` (Plan 03 will export it). Call with texts whose annotation contains a `{{token}}` literal; assert returned `ok === false` and `issues` includes a placeholder-related message. NOTE: `runCritic` is not exported yet — wrap this test so it asserts the export exists (`assert.strictEqual(typeof mod.runCritic, 'function')`) THEN the rule; the missing export makes it RED now.
    - Test "DEC-03 runCritic: topicKeywordCheck flags titleFull missing topic words" — via exported runCritic, pass texts whose titleFull contains none of the topic words and a topic string of e.g. 'Руки куклы, кисти рук'; assert `ok === false`.

    Keep all article IDs unique. Keep `force: true`.
  </action>
  <verify>
    <automated>node --test 'test/step-texts.test.js'; test $? -ne 0 &amp;&amp; echo RED-AS-EXPECTED || echo "WARN: expected failures"</automated>
  </verify>
  <acceptance_criteria>
    - `test/step-texts.test.js` contains a test asserting `body.needsReview === true` after a forced critic rejection (recursion termination).
    - The file contains a test asserting `history` last entry `attempts.length === 3`.
    - The file contains at least two tests invoking `require('../functions/step-texts/index.js').runCritic` (currently undefined → RED).
    - `node --test 'test/step-texts.test.js'` exits non-zero (RED) — implementation does not exist yet.
    - No use of dotted-path updateManifest assertions; manifest is read from disk via fs.
  </acceptance_criteria>
  <done>All Phase 2 behaviors have a corresponding failing test; suite exits non-zero, confirming a valid RED contract for Plans 02 and 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → handler | Test constructs `event.body` strings; handler must parse safely (already try/catch → 400) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | step-texts recursive retry | mitigate | This plan WRITES the test that proves recursion terminates at `attempt < MAX_ATTEMPTS` (=3) — the regression guard for the DoS vector. Verified by the "needsReview after MAX" test. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all deps builtin (node:test, node:assert, fs, path). RESEARCH Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `node --test 'test/step-texts.test.js'` discovers >0 tests.
- Suite is RED (exits non-zero) against current handler — this is the success condition for Wave 0.
- `npm test` still runs existing A/B/C tests (they remain GREEN; only the new file is RED).
</verification>

<success_criteria>
- `test/step-texts.test.js` exists and is the executable contract for TXT-01, TXT-02, TXT-03.
- Tests cover: 5-size generation, no unresolved placeholders, moldType=hands avoids 'лицо', recursion termination at MAX_ATTEMPTS with needsReview, attempts[] length === 3, runCritic topicKeywordCheck + noUnresolvedPlaceholders.
- Suite fails RED now; Plans 02 and 03 turn it GREEN.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

| Symbol / File | Kind | Created/Modified | Notes |
|---------------|------|------------------|-------|
| `test/step-texts.test.js` | test file | created | RED contract: createLine helper, runTexts helper, readManifest helper, tests for TXT-01/02/03 + runCritic rules |
</artifacts_produced>

<output>
Create `.planning/phases/02-working-texts-step/02-01-SUMMARY.md` when done.
</output>
