---
phase: 02-working-texts-step
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - functions/step-texts/index.js
autonomous: true
requirements: [TXT-03]
must_haves:
  truths:
    - "When the critic returns ok:false and attempt < MAX_ATTEMPTS, the handler calls itself directly (no YMQ) with attempt+1"
    - "Recursion terminates: at attempt === MAX_ATTEMPTS (3) the handler saves the result with needsReview:true instead of recursing"
    - "The manifest history entry's attempts[] array contains one verdict per attempt (length 3 after 3 rejected attempts)"
    - "The dead enqueueRetry function is removed — no YMQ dependency remains in the local retry path"
  artifacts:
    - path: "functions/step-texts/index.js"
      provides: "Recursive local retry replacing enqueueRetry; attemptsLog accumulation through recursion"
      contains: "exports.handler"
      min_lines: 200
  key_links:
    - from: "functions/step-texts/index.js (retry branch)"
      to: "functions/step-texts/index.js (exports.handler)"
      via: "self-recursive call wrapped as { body: JSON.stringify(msg) }"
      pattern: "exports\\.handler\\(\\{"
    - from: "functions/step-texts/index.js (save branch)"
      to: "manifest history.attempts"
      via: "msg.attemptsLog accumulated array written via updateManifest"
      pattern: "attemptsLog"
---

<objective>
Implement DEC-01: replace the no-op `enqueueRetry` with direct recursive `exports.handler` calls so the generator-critic loop actually retries locally, and accumulate per-attempt verdicts through the recursion so the manifest records every attempt (Success Criterion 4 / TXT-03).

This is the core retry vertical slice: after this plan a rejected text triggers a real second/third generation attempt, the loop terminates at MAX_ATTEMPTS=3 with needsReview:true, and `history[].attempts[]` holds all three verdicts.

Purpose: TXT-03 — generator-critic loop works locally without YMQ. Also closes the DoS threat (T-02-01): recursion must terminate at the guard `attempt < MAX_ATTEMPTS`.
Output: Modified `functions/step-texts/index.js` (recursion + attemptsLog accumulation + enqueueRetry deleted). Turns the recursion/attempts tests from Plan 01 GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-texts-step/02-CONTEXT.md
@.planning/phases/02-working-texts-step/02-RESEARCH.md
@.planning/phases/02-working-texts-step/02-PATTERNS.md
@functions/step-texts/index.js
@functions/api/index.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace enqueueRetry with recursive handler call and thread attemptsLog through recursion (per DEC-01)</name>
  <files>functions/step-texts/index.js</files>
  <read_first>
    - functions/step-texts/index.js (full — retry branch lines 89-91, save branch lines 60-87, enqueueRetry lines 221-239, buildAttemptsLog lines 260-263, parseMessage lines 245-258, updateManifest call line 81)
    - functions/api/index.js (runLocally lines 56-73 — shows the `{ body: JSON.stringify(msg) }` event-wrapping convention)
    - .planning/phases/02-working-texts-step/02-RESEARCH.md (Pitfall 3 — updateManifest is 3-arg `(article, stepId, patch)`, NOT dotted-path; Pitfall 4 — attempts accumulation; Security Domain — guard `attempt < MAX_ATTEMPTS`)
    - .planning/phases/02-working-texts-step/02-PATTERNS.md (retry-branch and save-branch replacement patterns, attemptsLog carry-forward)
  </read_first>
  <behavior>
    - Critic ok:false AND attempt < MAX_ATTEMPTS (3): handler recurses with attempt+1, carrying feedback=criticVerdict.issues and an accumulated attemptsLog.
    - Critic ok:false AND attempt === MAX_ATTEMPTS (3): handler saves artifact with needsReview:true and does NOT recurse (termination — DoS guard).
    - Critic ok:true at any attempt: handler saves with needsReview:false.
    - After 3 rejected attempts, the saved history entry's attempts[] has length 3, each with { attempt, criticVerdict }.
    - No call to enqueueRetry remains; the function is deleted.
  </behavior>
  <action>
    In `exports.handler`, destructure `attemptsLog` from the message alongside the existing fields: change the destructure to include `attemptsLog = []` (default empty array) so first call starts with an empty log.

    Replace the retry branch (current lines 89-91, the `enqueueRetry` + `respond(202)` block). New behavior: when `!criticVerdict.ok && attempt < MAX_ATTEMPTS`, return a recursive call to `exports.handler` with the event wrapped as `{ body: JSON.stringify({...}) }` (parseMessage reads event.body — a bare object breaks it, per Pitfall research). The inner message must carry: same `article`, same `size`, `attempt: attempt + 1`, `feedback: criticVerdict.issues`, `force` (preserve current value), and `attemptsLog: [...attemptsLog, { attempt, criticVerdict }]` (append THIS attempt's verdict before recursing).

    In the save branch (currently `criticVerdict.ok || attempt >= MAX_ATTEMPTS`), replace the `attempts:` value inside `historyEntry`. Instead of `buildAttemptsLog(stepMeta, attempt, criticVerdict)`, build the final attempts array as `[...attemptsLog, { attempt, criticVerdict }]` — this appends the FINAL attempt to the accumulated log so all attempts (1..N) are recorded. Keep using the 3-argument `store.updateManifest(article, STEP_ID, { currentVersion, history })` call exactly as-is (do NOT introduce any dotted-path patch — Pitfall 3). Do NOT add a separate updateManifest for needsReview: it is already persisted inside historyEntry and the artifact payload when attempt >= MAX.

    Delete the entire `enqueueRetry` function (current lines 221-239) — it has no other callers after this change. Remove its now-unused `@aws-sdk/client-sqs` require that lives inside it. Keep `buildAttemptsLog` ONLY if any other caller exists; grep confirms it is called only at line 78, which you are replacing, so DELETE `buildAttemptsLog` too (lines 260-263) to avoid dead code. (If grep shows another caller, keep it.)

    Do NOT change generateTexts, runCritic, parseMessage, the cache-check, or the LLM/stub fallback chain in this plan.
  </action>
  <verify>
    <automated>node --test 'test/step-texts.test.js' 2>&amp;1 | grep -Ev '^#' | grep -i 'attempts\|needsReview\|recur' ; node -e "const m=require('./functions/step-texts/index.js'); process.exit(typeof m.handler==='function'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `functions/step-texts/index.js` does NOT contain the string `enqueueRetry`.
    - `functions/step-texts/index.js` does NOT contain `would enqueue retry`.
    - The file contains `attemptsLog` (carried through recursion).
    - The file contains a self-recursive `exports.handler({` call wrapped with `body: JSON.stringify(`.
    - The save branch builds attempts as a spread of `attemptsLog` plus the current `{ attempt, criticVerdict }`.
    - The recursion-termination test and the attempts-length-3 test from Plan 01 PASS (`node --test 'test/step-texts.test.js'` shows those cases green; substitution/critic-rule tests may still be RED until Plan 03).
    - `store.updateManifest` is still called with exactly 3 arguments `(article, STEP_ID, {...})`.
  </acceptance_criteria>
  <done>Critic rejection recurses locally up to 3 attempts, terminates with needsReview:true, manifest history attempts[] length is 3, and enqueueRetry is gone. Plan 01 recursion + attempts tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message → recursive handler | Each recursion increments attempt; untrusted-loop risk if guard absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | step-texts recursive retry | mitigate | Recursion is guarded by `attempt < MAX_ATTEMPTS` (=3); at attempt 3 the save branch runs and returns instead of recursing. Termination proven by Plan 01 "needsReview after MAX" + "attempts length 3" tests. |
| T-02-02 | Tampering | message body parsing in recursion | mitigate | Recursive call wraps message as `{ body: JSON.stringify(msg) }`; parseMessage already try/catch → respond(400) on malformed input. No new parsing path introduced. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; this plan REMOVES an `@aws-sdk/client-sqs` require. Zero new packages (RESEARCH audit). |
</threat_model>

<verification>
- `node --test 'test/step-texts.test.js'`: recursion-termination and attempts-length tests GREEN.
- `grep -v '^#' functions/step-texts/index.js | grep -c enqueueRetry` returns 0.
- `npm test`: existing A/B/C regression tests remain GREEN.
</verification>

<success_criteria>
- TXT-03 satisfied: local generator-critic loop retries via direct recursion, no YMQ.
- Loop terminates at MAX_ATTEMPTS=3 with needsReview:true (DoS guard verified).
- Manifest history[].attempts[] records every attempt verdict (length 3 after 3 rejections).
- enqueueRetry and its SQS require are removed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Symbol / File | Kind | Created/Modified | Notes |
|---------------|------|------------------|-------|
| `exports.handler` (functions/step-texts/index.js) | handler | modified | Recursive retry branch + attemptsLog destructure + accumulated attempts in save branch |
| `enqueueRetry` (functions/step-texts/index.js) | function | removed | Dead no-op YMQ stub deleted; SQS require removed |
| `buildAttemptsLog` (functions/step-texts/index.js) | function | removed (if no other caller) | Superseded by attemptsLog carry-forward |
</artifacts_produced>

<output>
Create `.planning/phases/02-working-texts-step/02-02-SUMMARY.md` when done.
</output>
